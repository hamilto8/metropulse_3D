# Phase 2 Time and Weather Kernel Handoff

Chunk ID and status: `phase-2-time-weather-kernel`; complete as the twelfth Phase 2 slice. This completes the time/weather/celestial portion of Phase 2.5 dependency item 10; item 10 and Phase 2 overall remain in progress.

Source revision / Godot revision: Frozen browser reference `44a286a74557adfe4fabd3a6e16b9006079eba32`; implementation based on Godot revision `cef6e84` targeting Godot 4.6 stable .NET.

Objective and explicit non-goals: Port the renderer-independent environment clocks and orbit geometry: canonical weather sequence lookup, overflow-safe dynamic cycling, simulation-time speed/rate/wrap math, day/night transition factor, and sun/moon angles, positions, and horizon visibility. This chunk does not create lights, sky materials, fog, rain particles, lightning, bloom/exposure, UI controls, save scheduling, Godot nodes, or physics grip adapters.

Documents and source files read: `docs/GODOT_4_6_CSHARP_PORT_PLAN.md`, the Phase 0 and prior Phase 2 handoffs, `src/systems/Weather.js`, the pure exports in `src/systems/TimeManager.js`, `src/systems/TimeOfDayVisuals.js`, `src/world/CelestialOrbit.js`, `test/Weather.test.js`, `test/TimeManager.test.js`, `test/TimeOfDayVisuals.test.js`, `test/CelestialOrbit.test.js`, canonical `weather.json`, and the existing C# canonical weather registry/validators.

Files/scenes/resources added or changed: Added `MetroPulse.Domain/TimeWeather/WeatherCycleModel.cs`, `SimulationTimeModel.cs`, and `CelestialOrbitModel.cs`; added `TimeWeatherModelTests.cs`; and updated the port plan, parity matrix, and this handoff. No scene, light, particle, shader, input action, collision layer, browser source, fixture, canonical content record, or save schema changed.

Authoritative owners touched: `GameContentRegistry` remains the sole canonical weather scalar/sequence owner. `WeatherCycleModel` owns normalized mode lookup and dynamic clock math over an injected validated catalog. `SimulationTimeModel` owns accepted speed multipliers, base time rate, 24-hour wrapping, and twilight classification. `CelestialOrbitModel` owns renderer-neutral orbit angles/positions and horizon visibility. Future Godot environment adapters consume these results and must not duplicate their clocks or orbit formulas.

Public APIs, signals, events, input actions, collision layers: Added immutable weather-cycle state, time-of-day visual configuration, orbit configuration, horizontal center, angles, and position records; production weather model loading; normalize/definition/next/step weather operations; normalize/advance/hour/night-factor time operations; and celestial angle/position/visibility operations. No mutable singleton, engine signal, input action, or collision change was added.

Stable IDs/schema/content changes: Preserved `clear`, `mist`, `rain`, and `thunderstorm` declaration order; durations `45/30/40/28`; every canonical weather scalar/status string through the existing registry; default `clear`; time speeds `0.5/1/5/15`; one game minute per real second at 1x; 24-hour wrap; twilight boundaries `17/19/5/7`; orbit radius `1400`, lateral drift `260`, sun/moon radii `110/68`, far plane `2200`, and sunrise origin hour `6`. No serialized schema or content ID changed.

Behavior implemented: Invalid weather IDs normalize to clear; missing/nonpositive remaining time resets to the current mode duration; disabled cycling returns zero remaining; negative/nonfinite deltas cannot advance; frame overflow carries across modes; complete weather cycles fold without per-cycle iteration. Simulation time rejects unsupported multipliers, clamps negative/nonfinite elapsed time, and wraps safely. Night factor uses the exact Three.js smoothstep curve. Sun and moon remain opposite, use the browser's lateral-drift geometry, sanitize nonfinite numeric inputs, and become visible only when their top edge crosses the horizon.

Known deviations and ADR links: No behavior deviation requires an ADR. Typed finite doubles replace JavaScript numeric coercion. Three.js color interpolation and renderer exposure/bloom are presentation behavior and intentionally remain with the Phase 4/9 Godot environment adapter; this slice ports the pure twilight factor they consume. UI synchronization and save scheduling in `TimeManager` likewise remain Phase 3/9 adapter behavior.

Tests added and exact commands: Added eight xUnit cases, increasing the domain suite from 106 to 114. Coverage mirrors the pure frozen browser cases: canonical sequence/lookup, per-mode overflow, disabled/invalid/large-delta cycling, all time multipliers and malformed/wrapped time, twilight boundaries and smooth transitions, sunrise/sunset terrain clearance, opposite-body and malformed orbit values, and top-edge horizon visibility. Commands: `dotnet test godot/MetroPulse.Domain.Tests/MetroPulse.Domain.Tests.csproj --no-restore --filter FullyQualifiedName~TimeWeatherModelTests`, `dotnet test godot/MetroPulse.Domain.Tests/MetroPulse.Domain.Tests.csproj --no-restore`, `dotnet format godot/MetroPulse.Godot/MetroPulse.Godot.sln --no-restore --verify-no-changes`, `npm run baseline:check`, `npm run godot:content:check`, and `GODOT_BIN=/private/tmp/Godot46Mono/Godot_mono.app/Contents/MacOS/Godot ./godot/scripts/verify.sh`.

Test results and artifact paths: The focused time/weather suite passes `8/8` and the domain suite passes `114/114`. The full verification gate exits `0`: export preset identifiers validate; canonical extraction and Phase 0 baseline checks have zero drift; the deterministic .NET build completes with zero warnings and errors; Godot 4.6 stable .NET completes headless import; and all `18` foundation integration assertions pass. TRX output is `godot/artifacts/test-results/domain-tests.trx`.

Performance/resource counts before and after: Domain tests increased from 106 to 114. Runtime node/body/timer/input/collision counts remain unchanged at zero for this assembly. Weather stepping is constant work for complete cycles plus at most one pass through the four-mode sequence; time and orbit helpers are constant-time allocations/math.

Manual checks performed: None; this slice has no player-facing or engine-owned surface. Fixed-time/weather screenshot parity, sky/lighting readability, grip, fog/rain/lightning, controls, and save restoration remain later phases.

Open defects with severity and reproduction: None found in the ported surface.

Compatibility adapters and removal conditions: `WeatherCycleModel.LoadProduction` bridges the canonical content registry into the pure clock. The Phase 4 environment owner must use its output for rendering and the Phase 5/6 vehicle/traffic owners must use the same canonical grip values. The browser's environment setters remain compatibility evidence until Godot adapters prove UI/save synchronization. Do not copy weather durations, time multipliers, or orbit constants into scenes.

Next safe task: Continue Phase 2.5 item 10 with traffic navigation/rules and the aggregate `TrafficProductivityModel`, because alert, condition, economy, and mission consumers already depend on its immutable snapshot contract. Keep vehicle actors, physics queries, road-node scenes, and runtime spawning outside the domain assembly.

Unsafe/blocked tasks and required decision: Do not port Three.js light/color/material mutation into the domain assembly. Do not connect clocks directly to Godot `_Process`, UI, save repositories, or vehicle bodies before their phase adapters own pause and restore behavior. No external decision blocks the traffic helper slice.
