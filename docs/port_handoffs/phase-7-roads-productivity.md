# Phase 7 Roads, Bridges, and Productivity Handoff

Chunk ID and status: `phase-7-roads-productivity`; complete as the third of five Phase 7 slices. Work items 7.6 and 7.7 are complete; Phase 7 remains in progress.

Source revision / Godot revision: Frozen browser reference `44a286a74557adfe4fabd3a6e16b9006079eba32`; follows editor-transaction commit `36f0eb2`; targets Godot 4.6 stable .NET `89cea1439` and the existing 48-moving/12-parked traffic floor.

Objective and explicit non-goals: Replace the road participant placeholder with live route/deck/hazard ownership and bind the already ported frozen aggregate traffic model to Godot traffic, economy, alerts, and street presentation. Incident/outage mutation, street repair interactions, and durable save-envelope composition remain in the next slice; this slice consumes their existing optional domain contracts but does not create a second outcome owner.

Road topology ownership: `LivingTrafficRuntime.RoadGraph` remains the sole mutable authored and player topology. `TrafficRoadWorldEditParticipant` occupies the required `road-graph` transaction position and registers `ROAD_STRAIGHT`, `ROAD_INTERSECTION`, and `BRIDGE_DECK` records with stable `USER_BUILDING_n` road IDs. `TrafficRoadGraph.GetRoadNetworkSnapshot()` derives connected/disconnected status from current edges rather than caching it, so later segments can connect an earlier isolated segment. Ordinary roads use the frozen 38-metre connection radius; bridge segments use an explicit 80-metre shore-link radius.

Dynamic-agent contract: `TrafficPopulationSimulation` watches graph revision. On additions it exposes new nodes on the next physics advance. On removal it repairs any agent whose current or target node disappeared by choosing the stable nearest routable node, clearing stuck time, and preserving the 48-agent floor. Default method parameters keep all existing isolated traffic tests byte-for-byte behavior-compatible.

Bridge physical-safety contract: Only the canonical `BRIDGE_DECK` road type bypasses water placement rejection. Over raw river aim is conformed to deck datum zero. The road participant adds one `SurfaceDeck` whose bounds equal the oriented physical footprint and whose height equals the top of the live visual/collider. `WorldSurfaceModel.IsWater` therefore returns false at supported deck height only inside that footprint. Detach/demolish unregisters the route and deck; river hazard immediately returns. Duplicate deck IDs fail closed, and the surface owner exposes explicit register/unregister results.

Aggregate productivity flow: `LivingTrafficRuntime` creates one `TrafficProductivityModel` after `CityEconomyRuntime` exists, provides the live road-network snapshot, registers `traffic.productivity` on the city stage, and publishes model output through `EconomyLedger.SetMobilityFeedback`. World-edit completion forces a final refresh after all eight participants commit so jobs, demand, topology, and economy cannot remain one tick out of sync. Ambient simulation consumes aggregate ordinary/bridge speed multipliers and Freight Priority route preference; sampled vehicle congestion never feeds the aggregate.

Policy, alert, mission, and presentation contracts: Balanced and Freight Priority remain the only bridge policies. Freight Priority provides the frozen +28% capacity and +10 reliability points for 2 credits/second (120/minute) and −2 satisfaction. `TrafficAlertAdapter` owns deduplicated congestion, bridge-disruption, and delivery-backlog alerts. Mission callers retain the same crossing availability, difficulty, time, reward, and delivery-demand projection. `TrafficProductivityPresenter` draws cyan primary-bridge chevrons only while priority is active and red disruption beacons only for restriction/outage snapshots. Player-road rings are green when connected, amber when isolated, and cyan for bridge deck segments.

Verification: The domain suite passes 297/297, including dynamic deck hazard restoration, connected custom bridge topology, graph add/remove refresh against a live 48-agent simulation, and bridge-only water placement. The Godot solution builds with zero warnings/errors and format verification passes. Clean headless integration passes 174 assertions and publishes `phase7.roads_productivity.passed`: the live straight-road transaction adds/removes 12 capacity; the custom bridge adds/removes exactly three nodes and one deck; bridge water safety reverses on demolition; mobility feedback matches the economy; Freight Priority updates cost/capacity/reliability, the street directive, and visible chevrons.

Known balancing deviation: The initial authored economy baseline is still frozen and recorded before mobility binding. Once the live aggregate model binds, recurring income, job access, satisfaction, and City Pulse legitimately consume current congestion. Integration now tests an exact one-second tick against that current derived net rate instead of continuing to require the pre-mobility 8 credits/second after work item 7.7 is active.

Next safe task: Implement `CityServiceModel` and `IncidentResponseService` against the existing economy/outcome/alert contracts, including spatial reach/falloff, funded cleanup-before-repair, Street interactions/markers, and a versioned save/reload projection for buildings, zones, roads, mobility policy, services, and incidents.
