# Phase 2 Aircraft Kernel Handoff

Chunk ID and status: `phase-2-aircraft-kernel`; complete as the seventeenth Phase 2 slice. This completes Phase 2.5 dependency item 10; item 2.6, the pure-family exit audit, and Phase 2 overall remain in progress.

Source revision / Godot revision: Frozen browser reference `44a286a74557adfe4fabd3a6e16b9006079eba32`; implementation based on Godot revision `e7f16b9` targeting Godot 4.6 stable .NET.

Objective and explicit non-goals: Port sanitized aircraft state/controls, deterministic arcade flight stepping, takeoff/taxi/bank/stall/altitude/landing/crash transitions, propeller rotation and audio telemetry, immutable airfield layout, authored/placed road classification, bridge exclusion, seven-point landing footprint, terrain grade, and water assessment. This chunk does not enable the default-off aircraft feature, create `RigidBody3D` or visual aircraft/airfield nodes, bind input, own control/camera transitions, query a Godot physics world, play audio, render instruments/effects, persist flight, or recover crashes.

Documents and source files read: `docs/GODOT_4_6_CSHARP_PORT_PLAN.md`, Phase 0 and prior Phase 2 handoffs, `src/entities/AircraftFlightModel.js`, `PropellerAircraft.js`, `src/systems/AircraftLandingSurface.js`, `AircraftAudio.js`, relevant pure boundaries in `AircraftSystem.js`, `src/world/Airfield.js`, all four matching test files, canonical `CountrysidePlan.js` evidence, and the existing typed C# countryside registry.

Files/scenes/resources added or changed: Added `MetroPulse.Domain/Aircraft/AircraftFlightModel.cs`, `AircraftLandingSurfaceModel.cs`, and `AircraftAudioModel.cs`; added `AircraftModelTests.cs`; updated the port plan, parity matrix, and this handoff. No browser source, canonical data, fixture, scene, node, input action, collision layer, feature flag, or save schema changed.

Authoritative owners touched: `AircraftFlightModel` owns normalized flight state, control authority, bounded time steps, arcade force/drag response, heading/attitude, takeoff, stall, ceiling, touchdown/crash, and propeller-rate math. `AircraftLandingSurfaceModel` owns immutable airfield/runway coordinates, canonical countryside-road adaptation, injected placed-road and bridge snapshots, footprint rotation, landing classification, water, and grade safety. `AircraftAudioModel` owns deterministic propeller/engine/airflow telemetry. Future Godot actors own bodies, input, rendering, audio nodes, collision observations, control handoff, and recovery.

Public APIs, signals, events, input actions, collision layers: Added stable aircraft modes, landing types/reasons, vector/planar point, config/state/control/environment, audio profile, airfield bounds/layout/spawn, placed road, landing world/assessment records; state/control normalization, flight step, propeller helpers, audio profile, production landing model, airfield validation, surface classification, footprint sampling, and landing assessment. No signal, event, timer, mutable singleton, input action, or collision layer was added.

Stable IDs/schema/content changes: Preserved modes `PARKED/TAXI/TAKEOFF/AIRBORNE/LANDING/CRASHED`; landing types `RUNWAY/ROAD/COUNTRYSIDE/UNSUITABLE`; reasons `invalid-position/water/unsupported-surface/terrain-too-steep`; flight constants `1.15/64/24/18/35/280`, pitch `0.16π`, roll `0.27π`, throttle/turn rates `0.52/0.62/0.78`, safe descent `−8`, safe roll `0.16π`; airfield center `−105,−260`, runway `28×210`, heading `π`, spawn `−105,1.15,−190`, bounds `−196..−18/−382..−138`; maximum grade `0.22`; all seven footprint points; and procedural audio formulas. No serialized content or state was introduced.

Behavior implemented: Inputs and state reject nonfinite telemetry, control authority clamps, delta caps at `0.1`, and heading wraps. Ground motion applies taxi steering, braking, throttle ramp, assisted rotation, and non-teleport takeoff. Airborne motion applies banked turn authority, drag/climb penalty, stall sink/warning, landing approach state, a 280-metre AGL ceiling, safe touchdown thresholds, and hard/water/unsupported crash outcomes. The runway, existing city/countryside roads, rocket access, mission spur, and non-bridge placed roads are valid landing families; bridge decks and unsupported urban land are rejected. Assessment samples gear, nose/tail, and wing tips, sanitizes terrain, checks every point for water, and bounds grade. Audio and propeller outputs are deterministic pure telemetry.

Known deviations and ADR links: No behavior deviation requires an ADR. Typed immutable records and injected delegates/snapshots replace open objects and direct `CityBuilder` access. Nonfinite placed-road rotation and invalid footprint sizes safely normalize rather than producing unusable comparisons. Airfield layout is code-owned parity data because Phase 2.4 did not designate it as a canonical extraction artifact; Godot scenery remains a Phase 4 asset decision. Aircraft remains default-off exactly as required by the MVP feature gate.

Tests added and exact commands: Added twelve xUnit cases, increasing the domain suite from 158 to 170. Coverage mirrors fourteen browser cases and adds numeric parity/boundaries: state/control sanitization; exact seven-second takeoff, two-second bank, and half-second stall telemetry; gentle/hard/water/unsupported touchdown; delta cap, ceiling, stable crash, propeller wrap; airfield bounds; runway/city/countryside/placed-road/bridge classification; immutable rotated seven-point footprint; level and rolling terrain; water/grade/unsupported/malformed failures; and audio telemetry/crash cutoff. Commands: `node --test test/AircraftFlightModel.test.js test/AircraftLandingSurface.test.js test/AircraftAudio.test.js test/Airfield.test.js`, `dotnet test godot/MetroPulse.Domain.Tests/MetroPulse.Domain.Tests.csproj --no-restore --filter FullyQualifiedName~AircraftModelTests`, `dotnet test godot/MetroPulse.Domain.Tests/MetroPulse.Domain.Tests.csproj --no-restore`, `dotnet format godot/MetroPulse.Godot/MetroPulse.Godot.sln --no-restore --verify-no-changes`, `npm run baseline:check`, `npm run godot:content:check`, and `GODOT_BIN=/private/tmp/Godot46Mono/Godot_mono.app/Contents/MacOS/Godot ./godot/scripts/verify.sh`.

Test results and artifact paths: The focused aircraft suite passes `12/12`, all four browser reference files pass `14/14`, and the domain suite passes `170/170`. The full verification gate exits `0`: export preset identifiers validate; canonical extraction and Phase 0 baseline checks have zero drift; the deterministic .NET build completes with zero warnings and errors; Godot 4.6 stable .NET completes headless import; and all `18` foundation integration assertions pass. TRX output is `godot/artifacts/test-results/domain-tests.trx`.

Performance/resource counts before and after: Domain tests increased from 158 to 170. Runtime node/body/timer/input/collision/audio counts remain unchanged at zero for this assembly. Flight and audio steps are constant-time. Surface classification is linear in small authored/placed road lists; landing assessment is fixed at seven footprint samples.

Manual checks performed: None; aircraft is default-off and this slice has no player-facing or engine-owned surface. Flight feel, runway readability, live terrain/road/bridge queries, collision envelope, control transitions, camera, sound, instruments, effects, recovery, persistence, and feature-gate acceptance remain later-phase checks.

Open defects with severity and reproduction: None found in the ported surface.

Compatibility adapters and removal conditions: `AircraftLandingSurfaceModel.LoadProduction` bridges the canonical countryside grid. Injected terrain/water/bridge/placed-road facts remain the boundary until Godot world and road owners expose authoritative snapshots. Godot aircraft and audio adapters must consume returned state/telemetry without reproducing formulas. Browser aircraft modules remain compatibility evidence until the optional feature is deliberately enabled and live scenario/playtest acceptance passes.

Next safe task: Implement Phase 2.6 named deterministic random streams, then migrate the explicitly deferred pedestrian descriptor/behavior samples and any other gameplay-relevant random consumers. Finish with the Phase 2.7 pure-family/parity-matrix audit and complete exit-gate verification.

Unsafe/blocked tasks and required decision: Do not enable the aircraft feature, instantiate physics/render/audio nodes, bind controls, create an airfield scene, own state transitions, or add flight persistence during Phase 2. Do not introduce `Random.Shared` or unnamed `System.Random`. No external decision blocks the named-stream slice.
