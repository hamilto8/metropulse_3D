# Phase 2 Named Random Streams Handoff

Chunk ID and status: `phase-2-random-streams`; complete as the eighteenth Phase 2 slice. This completes item 2.6; the Phase 2.7 pure-family exit audit and Phase 2 overall remain in progress.

Source revision / Godot revision: Frozen browser reference `44a286a74557adfe4fabd3a6e16b9006079eba32`; implementation based on Godot revision `5a5af04` targeting Godot 4.6 stable .NET.

Objective and explicit non-goals: Introduce isolated named deterministic streams, preserve the browser xmur3/mulberry32 bit sequence, persist only gameplay-relevant positions, and move the pure pedestrian randomness deferred by Phase 2.5 onto explicit streams. This chunk does not install a global RNG, mutate browser `Math.random`, wire the Godot composition root, create the aggregate save envelope, choose production world seeds, migrate engine-owned spawning/render/audio randomness, or implement Phase 3 persistence adapters.

Documents and source files read: `docs/GODOT_4_6_CSHARP_PORT_PLAN.md`, the Phase 0 and prior Phase 2 handoffs, `src/app/RuntimeConfig.js`, `src/entities/PedestrianArchetypes.js`, `src/systems/NpcBehavior.js`, `TrafficPedestrianBehavior.js`, `PedestrianImpact.js`, their matching tests, all current C# domain randomness consumers, and the production pedestrian content registry.

Files/scenes/resources added or changed: Added `MetroPulse.Domain/Randomness/DeterministicRandom.cs`, `MetroPulse.Domain/Pedestrians/PedestrianDescriptorModel.cs`, and `MetroPulse.Domain.Tests/Randomness/RandomStreamTests.cs`; updated `NpcBehaviorModel.cs`, `PedestrianTrafficModel.cs`, `PedestrianKnockdownModel.cs`, the port plan, parity matrix, and this handoff. No browser source, canonical data, fixture, scene, node, input action, collision layer, or aggregate save schema changed.

Authoritative owners touched: `RandomStreamRegistry` owns the supported stream names, seed derivation, stream isolation, draw ordering, typed access, gameplay persistence policy, and atomic restore validation. `DeterministicRandomStream` owns xmur3 seed hashing and mulberry32 advancement. `PedestrianDescriptorModel` owns canonical archetype sequencing and randomized appearance selection. Existing NPC, knockdown, and driver-disposition models now own explicit stream consumption while preserving their deterministic-sample overloads for fixtures.

Public APIs, signals, events, input actions, collision layers: Added `RandomStreamNames`, `IRandomStream`, `RandomStreamState`, `RandomStreamRegistryState`, `DeterministicRandomStream`, `RandomStreamRegistry`, `PedestrianAppearance`, `PedestrianDescriptor`, and `PedestrianDescriptorModel`. Added named-stream overloads for NPC state/tourist/aggression timing, knockdown start, and pedestrian/traffic encounters. No signal, event, timer, mutable singleton, input action, or collision layer was added.

Stable IDs/schema/content changes: The exact stream IDs are `WorldGeneration`, `TrafficSpawn`, `TrafficBehavior`, `PedestrianSpawn`, `PedestrianBehavior`, `Weather`, `Mission`, and `Cosmetic`. State schema version `1` stores the root seed plus `state` and `drawCount` for the seven gameplay streams; `Cosmetic` is intentionally excluded. Seeds derive per stream from `<root seed>\u001f<stream name>`. The default seed remains `metropulse-phase-0`. Pedestrian archetype sequence, colors, skin/hair/pants palettes, accessories, and height formula remain browser-exact. No canonical content value changed.

Behavior implemented: Equal root seeds produce equal per-stream sequences; a draw from one stream cannot advance another. Unknown or incorrectly assigned stream names fail actionably. Each valid helper draw advances exactly once. Snapshot output is immutable and contains exactly seven gameplay streams. Restore validates schema, seed, exact stream membership, and non-null snapshots before constructing or mutating a registry. Cosmetic output restarts from the root seed after restore. Pedestrian descriptors consume exactly six spawn samples; NPC timing and knockdown consume only `PedestrianBehavior`; driver impatience consumes only `TrafficBehavior`. Safe sample fallbacks preserve the browser helper contracts for injected failing sources.

Known deviations and ADR links: No behavior deviation requires an ADR. Browser test mode replaces the global `Math.random`; the Godot domain instead derives isolated named streams from the same algorithm because item 2.6 explicitly strengthens deterministic ownership. A unit-separator seed delimiter prevents ambiguous seed/name concatenation. The new random state is only a Phase 3 save subdocument; the aggregate save envelope remains unchanged. Engine and presentation randomness stays with later adapters and must consume the appropriate named stream or explicitly use non-persisted `Cosmetic`.

Tests added and exact commands: Added nine xUnit cases, increasing the domain suite from 170 to 179. Coverage includes an exact browser-compatible golden vector; eight-stream repeatability, seed sensitivity, isolation, names, and unknown rejection; helper ranges and draw counts; immutable JSON round-trip; exact gameplay-only persistence; Cosmetic restart; corrupt schema/seed/member rejection; exact descriptor samples and 20-entry archetype mix; ordered NPC/knockdown draws; traffic disposition isolation; and wrong-stream rejection. Commands: `node --test test/RuntimeConfig.test.js test/NpcBehavior.test.js test/TrafficManagement.test.js test/GameplaySystems.test.js`, `dotnet test godot/MetroPulse.Domain.Tests/MetroPulse.Domain.Tests.csproj --no-restore --filter FullyQualifiedName~RandomStreamTests`, `dotnet test godot/MetroPulse.Domain.Tests/MetroPulse.Domain.Tests.csproj --no-restore`, `dotnet format godot/MetroPulse.Godot/MetroPulse.Godot.sln --no-restore --verify-no-changes`, `npm run baseline:check`, `npm run godot:content:check`, and `GODOT_BIN=/private/tmp/Godot46Mono/Godot_mono.app/Contents/MacOS/Godot ./godot/scripts/verify.sh`.

Test results and artifact paths: The focused named-stream suite passes `9/9`, the four browser reference files pass `43/43`, and the domain suite passes `179/179`. The full verification gate exits `0`: export preset identifiers validate; canonical extraction and Phase 0 baseline checks have zero drift; the deterministic .NET build completes with zero warnings and errors; Godot 4.6 stable .NET completes headless import; and all `18` foundation integration assertions pass. TRX output is `godot/artifacts/test-results/domain-tests.trx`.

Performance/resource counts before and after: Domain tests increased from 170 to 179. Runtime node/body/timer/input/collision/audio counts remain unchanged at zero for this assembly. Stream draws, snapshots, and fixed pedestrian descriptors are bounded; registry construction and restore are linear in exactly eight and seven entries respectively.

Manual checks performed: None; this slice has no player-facing or engine-owned surface. Production seed selection, save integration, spawn ordering, cross-save continuation, world/traffic/weather/mission adapters, and presentation-only Cosmetic usage remain later integration checks.

Open defects with severity and reproduction: None found in the ported surface.

Compatibility adapters and removal conditions: Deterministic-sample overloads remain for frozen fixture parity and pure callers. Godot CompositionRoot will own a session registry in Phase 3 and inject streams; its persistence service will embed `RandomStreamRegistryState` atomically in the aggregate envelope. Later world, traffic, pedestrian, weather, mission, and presentation adapters must request a named stream instead of constructing `System.Random` or using `Random.Shared`. Browser `RuntimeConfig` remains reference evidence until browser retirement.

Next safe task: Execute Phase 2.7 as a complete pure-browser-family audit: map every remaining pure test family to a C# owner or document its engine-adapter phase, close parity-matrix gaps, then rerun the Phase 2 exit gate.

Unsafe/blocked tasks and required decision: Do not introduce a mutable global registry, persist Cosmetic state, silently accept unknown/missing stream snapshots, or wire Phase 3 save/world adapters into this pure slice. No external decision blocks the Phase 2.7 audit.
