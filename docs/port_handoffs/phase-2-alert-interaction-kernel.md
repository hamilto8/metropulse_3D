# Phase 2 Alert and Interaction Kernel Handoff

Chunk ID and status: `phase-2-alert-interaction-kernel`; complete as the eighth Phase 2 slice. Phase 2.5 dependency item 6 is complete; Phase 2 overall remains in progress.

Source revision / Godot revision: Frozen browser reference `44a286a74557adfe4fabd3a6e16b9006079eba32`; implementation based on Godot revision `c8b84b6` targeting Godot 4.6 stable .NET.

Objective and explicit non-goals: Port structured alert lifecycle/persistence and deterministic interaction selection/resolution as renderer-independent C# authorities, then bind the already-ported economy authority through a fiscal alert adapter. This chunk does not build UI, input/raycast adapters, runtime interaction providers, the traffic alert publisher, the service-work provider, mission outcome transactions, save repositories, or Godot nodes.

Documents and source files read: `docs/GODOT_4_6_CSHARP_PORT_PLAN.md`, the Phase 0 and Phase 2 handoffs, `src/alerts/AlertService.js`, `src/systems/InteractionService.js`, `src/systems/EconomyAlertAdapter.js`, `src/systems/TrafficAlertAdapter.js`, `src/systems/ServiceWorkInteractionProvider.js`, `test/AlertService.test.js`, `test/InteractionService.test.js`, `test/EconomyRecovery.test.js`, `test/TrafficProductivityModel.test.js`, `test/ServiceWorkInteractionProvider.test.js`, and the unchanged Phase 0 `settings-bindings-alerts.json` fixture.

Files/scenes/resources added or changed: Added `MetroPulse.Domain/Alerts/AlertContracts.cs`, `AlertService.cs`, and `EconomyAlertAdapter.cs`; added `MetroPulse.Domain/Interactions/InteractionService.cs`; added matching alert and interaction test families; embedded the unchanged Phase 0 settings/bindings/alerts fixture in the test assembly; and updated the port plan, parity matrix, and this handoff. No scene, input action, collision layer, browser source, authored content, or fixture changed.

Authoritative owners touched: `AlertService` solely owns alert normalization, lifecycle, ordering, revision, history, and state documents. `InteractionService` solely owns candidate normalization, deterministic ranking, primary selection, action resolution, and immutable snapshots. `EconomyLedger` remains the fiscal authority; `EconomyAlertAdapter` only translates its snapshots into one deduplicated alert.

Public APIs, signals, events, input actions, collision layers: Added typed alert input/record/location/duration/focus/state/snapshot/event contracts and stable token catalogs; publish, legacy publish/migration, lookup, resolve, timed expiry, snapshot, subscribe, serialize, validate, restore, and destroy operations; interaction priority and resolution-status catalogs; typed candidate/context/snapshot/resolution contracts; provider registration, refresh, primary resolution, subscription, clearing, normalization, comparison, and selection operations. C# delegates return idempotent unregister/unsubscribe functions. No engine signal, input action, or collision layer was added.

Stable IDs/schema/content changes: Preserved alert record version `1`, state-document version `2`, uppercase type/severity/state/duration/focus tokens, caller IDs/dedupe keys, and the browser's `alert-<sequence>` default ID convention. Preserved interaction resolution tokens `NONE`, `COMPLETED`, `INELIGIBLE`, `ACTION_REJECTED`, and `ACTION_FAILED`, plus priorities `1000`, `950`, `900`, `800`, `700`, `600`, `500`, and `100`. Camel-case JSON round-trip is verified. No authored ID, source schema, or save envelope changed.

Behavior implemented: Active alerts deduplicate by stable key while retaining original start time and incrementing occurrence count; explicit supersession and resolution preserve history; timed alerts expire from `lastObservedAt`; active-first severity/recency sorting and inactive-only history trimming match the browser; complete state validation precedes restore mutation; legacy messages migrate to structured alerts; and listener failures cannot break authority. Fiscal stress publishes and updates one warning/critical alert, including explanation, cashflow, restrictions, grant remedy, and stable resolution. Interaction candidates require prompt, accessibility label, eligibility, failure reason where blocked, finite priority, non-negative distance, and action; selection is eligible first, then priority descending, distance ascending, candidate ID, and provider ID. Refresh isolates context/provider/listener failures; resolution distinguishes blocked, rejected, failed, completed, and reentrant-none outcomes and refreshes after actions.

Known deviations and ADR links: No behavior deviation or ADR is required for the ported surface. C# exposes typed records and read-only collections rather than untyped JavaScript objects. The service catches provider/action/context/listener exceptions and reports them through the injected error hook, matching browser isolation without owning a logger.

Tests added and exact commands: Added six `AlertServiceTests` cases and seven `InteractionServiceTests` cases. Alert coverage includes the exact frozen publish/deduplicate/active/resolve/serialize lifecycle, bounded history, supersession, timed expiry, JSON restore, legacy migration, atomic malformed-state rejection, ordering, listener isolation, and fiscal adapter lifecycle. Interaction coverage includes contract validation, stable priority order, eligibility/distance/ID tie breaks, immutable multi-provider snapshots, blocked/completed/rejected/failed resolution, failure isolation, and reentrancy. Commands: `dotnet build godot/MetroPulse.Domain.Tests/MetroPulse.Domain.Tests.csproj --no-restore --verbosity:minimal`, `./godot/scripts/test-domain.sh`, `dotnet format godot/MetroPulse.Godot/MetroPulse.Godot.sln --no-restore --verify-no-changes`, `npm run baseline:check`, `npm run godot:content:check`, and `GODOT_BIN=/private/tmp/Godot46Mono/Godot_mono.app/Contents/MacOS/Godot ./godot/scripts/verify.sh`.

Test results and artifact paths: The full verification gate exits `0`: export preset identifiers validate; canonical extraction and Phase 0 baseline checks have zero drift; the deterministic .NET build completes with zero warnings and errors; the domain suite passes `76/76`; Godot 4.6 stable .NET completes headless import; and all `18` foundation integration assertions pass. TRX output is `godot/artifacts/test-results/domain-tests.trx`. The frozen alert lifecycle comparison has zero structural or scalar drift.

Performance/resource counts before and after: Domain tests increased from 63 to 76. Runtime node/body/timer/input/collision counts remain unchanged at zero for this assembly. Alert work is bounded by the configured record cap (minimum 10, default 100); interaction refresh scans the registered providers and sorts their current candidates. Published collections and context/metadata maps are copied into read-only views.

Manual checks performed: None; this slice has no player-facing or engine-owned surface. Manual UI/input acceptance remains with Phases 5, 8, and 9.

Open defects with severity and reproduction: None found in the ported surface.

Compatibility adapters and removal conditions: `EconomyAlertAdapter` remains a narrow dual-engine translation boundary until the browser reference is retired and the Godot UI consumes `AlertSnapshot` directly. The Phase 0 fixture remains read-only evidence. Removal requires an accepted authority replacement and refreshed parity evidence; presentation code must not reproduce fiscal thresholds or alert lifecycle.

Next safe task: Begin Phase 2.5 item 7 with pure mission outcome transactions and city-condition queries. Bind outcome capital/reputation/service/incident changes through the existing atomic ledger and stable completion IDs, without adding mission lifecycle or Godot scene behavior yet.

Unsafe/blocked tasks and required decision: Do not port `TrafficAlertAdapter` until Phase 2.5 item 10 owns `TrafficProductivityModel` snapshots, and do not port `ServiceWorkInteractionProvider` until the incident-response/outcome authority has stable service-work intent and completion contracts. Neither deferral requires a product decision; implementing either early would create a second authority or test-only substitute.
