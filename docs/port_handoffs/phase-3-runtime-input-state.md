# Phase 3 Runtime Input State Handoff

Chunk ID and status: `phase-3-runtime-input-state`; complete as the third Phase 3 slice. Phase 3 remains in progress.

Source revision / Godot revision: Frozen browser reference `44a286a74557adfe4fabd3a6e16b9006079eba32`; implementation continues from Godot revision `d7fc008` and targets Godot 4.6 stable .NET.

Objective and explicit non-goals: Publish one immutable, contextual input snapshot at the beginning of every interactive physics tick while preserving browser device switching, dead zones, edge detection, focus/settings/context clearing, held-input quarantine, suspension, and prompt metadata. This chunk deliberately does not move a camera, player, vehicle, or aircraft; implement pause/dialogue UI; add controller remapping; implement toggle/hold preferences; or add save persistence.

Documents and source files read: The complete Phase 3 plan and first two handoffs; `GODOT_INPUT_ACTIONS.md`; `GAME_STATE_MACHINE.md`; `PAUSE_AND_MODAL_BEHAVIOR.md`; browser `InputManager.js` and `ControlBindings.js`; native settings authority/InputMap adapter, composition root, session shell, diagnostics runner, and simulation scheduler contracts.

Files/scenes/resources added or changed: Added renderer-independent `RuntimeInputState`/snapshot/context/prompt/action-ID contracts and tests; added session-owned `RuntimeInputHost`; extended `GodotInputMapAdapter` with reverse physical-token mapping and direct fixed-gamepad sampling; wired input construction/readiness/disposal through `SessionShell` and `CompositionRoot`; expanded headless integration; updated the port plan, InputMap contract, parity matrix, and this handoff. No browser source or gameplay body changed.

Authoritative owners touched: `SettingsStore` remains the only binding authority. `GodotInputMapAdapter` remains the only `metropulse_` InputMap action owner. `RuntimeInputState` owns transient context, interface, suspension, quarantine, edge history, and immutable snapshot publication without referencing Godot. `RuntimeInputHost`, owned at `SessionRoot/RuntimeServices/RuntimeInputHost`, translates Godot events/polling into one domain sample per physics callback. `CompositionRoot` still owns construction order and cannot release the session unless the runtime input owner is initialized.

Public APIs, signals, events, input actions, collision layers: Added `RuntimeInputHost.Initialize`, `SetContextSignals`, `Suspend`, `Resume`, `ClearAndQuarantine`, `LatestSnapshot`, and `PhysicsSnapshotCount`; `RuntimeInputState.ObserveActivity`, `FreezePhysicsTick`, suspension/clearing methods, thresholds, and snapshot records; `GodotInputMapAdapter.TryGetKeyboardMouseToken` and `GetGamepadActionStrength`. Snapshot slot IDs are `<ACTION>#SLOT_<index>`. Physics priority is `-1000`. No Godot signal, autoload, new InputMap action, or collision layer was added.

Stable IDs/schema/content changes: Settings schema remains version 2, save/content schemas are unchanged, and all existing context/action/input tokens remain exact. Active-interface tokens remain `KEYBOARD` and `GAMEPAD`. Context priority is Pause, Dialogue, Builder, Vehicle, Aircraft, Pedestrian, Management. Gamepad activity remains `0.32`, analog dead zone remains `0.15`, and edge pressed threshold is `0.5`.

Behavior implemented: Keyboard/mouse and standard-gamepad activity switch live authority. Inputs held before settings, focus, context, suspension, or device changes cannot leak through; keyboard inputs remain quarantined until their individual releases and gamepad input until neutral. Disconnect returns authority to keyboard. Focus/app pause clears pointer and edge history. Every interactive physics callback samples aggregate actions, keyboard slots, pointer delta, sticks, triggers, connection/quarantine/suspension flags, and current prompt labels. Published dictionaries and edge sets are read-only/frozen. Session boot keeps the host disabled until final readiness and interactive release.

Known deviations and ADR links: No parity deviation requires an ADR. Godot engine events replace browser DOM events while round-tripping to the same validated physical tokens. Gamepad bindings remain fixed and non-remappable. Controller hardware matrix/manual prompt rendering remains later acceptance work rather than a runtime-contract deviation.

Tests added and exact commands: Added nine `RuntimeInputStateTests` covering modal/context priority, dead-zone parity, keyboard and gamepad switching/quarantine, focus clearing and release edges, context/suspension leakage prevention, prompt/binding updates, immutable normalized snapshots, and stable slot IDs. Expanded native integration from 39 to 47 assertions for session ownership, readiness, physics priority/cadence, Management keyboard authority, prompt metadata, stable slot publication, and physical token round-trip. Commands: `dotnet test godot/MetroPulse.Domain.Tests/MetroPulse.Domain.Tests.csproj --no-restore`; `dotnet build godot/MetroPulse.Godot/MetroPulse.Godot.csproj --no-restore --no-incremental -v:minimal`; `dotnet format godot/MetroPulse.Godot/MetroPulse.Godot.sln --no-restore --verify-no-changes`; `GODOT_BIN=/Applications/Godot_mono.app/Contents/MacOS/Godot ./godot/scripts/test-integration.sh`; `GODOT_BIN=/Applications/Godot_mono.app/Contents/MacOS/Godot ./godot/scripts/verify.sh`; and `npm test`.

Test results and artifact paths: The native build succeeds with zero warnings/errors; browser tests pass 389/389; domain tests pass 194/194; format verification passes; and all 47 headless integration assertions pass. The complete `godot/scripts/verify.sh` gate exits 0, including export-preset validation, zero canonical-content drift, the 62-file ownership audit across 72 domain files, build, domain tests, headless import, and integration. Domain TRX output is `godot/artifacts/test-results/domain-tests.trx`; headless import and integration logs are `godot/artifacts/test-results/godot-import.log` and `godot/artifacts/test-results/godot-integration.log`.

Performance/resource counts before and after: Added one session child node and one settings listener. Input is sampled once per physics callback at the earliest declared priority, never once per gameplay consumer. Each immutable publication contains only the active context's 3–10 aggregate actions, its keyboard slot values when keyboard owns input, two edge sets, two stick vectors, two triggers, and 3–10 prompt strings. No body, timer, audio player, render resource, collision object, or per-frame scene-tree lookup was added.

Manual checks performed: Inspected structured boot output and confirmed the input node is constructed while the session is disabled, required by final readiness, then samples only after interactive release. Headless integration exercised real Godot physics ordering and event-token translation. No physical controller or player-facing prompt Control exists yet, so hardware/visual accessibility review is deferred.

Open defects with severity and reproduction: None found in this slice. Toggle/hold preference application, modal UI consumption, game-save scenarios, expanded diagnostics, and physical-controller matrix acceptance remain planned work.

Compatibility adapters and removal conditions: Browser `InputManager` remains frozen comparison evidence until native gameplay consumers, pause/dialogue/settings UI, physical controller checks, and Phase 11 acceptance pass. `RuntimeInputHost` is the target adapter; later gameplay nodes must consume `LatestSnapshot` and must not introduce raw key state or a second prompt/device authority.

Next safe task: Implement Phase 3 rotating current/recovery/temporary game-save repository and exact versioned envelope/migrations with fault injection, then insert save discovery/application at their stable boot IDs.

Unsafe/blocked tasks and required decision: Do not add raw key reads to gameplay nodes, treat InputMap as settings authority, serialize engine events, implement controller remapping, release input before readiness, or claim physical-controller/prompt presentation acceptance from headless tests. No external decision blocks the next persistence slice.
