# Phase 2 Camera Kernel Handoff

Chunk ID and status: `phase-2-camera-kernel`; complete as the fourteenth Phase 2 slice. This completes the renderer-independent camera-helper portion of Phase 2.5 dependency item 10; item 10 and Phase 2 overall remain in progress.

Source revision / Godot revision: Frozen browser reference `44a286a74557adfe4fabd3a6e16b9006079eba32`; implementation based on Godot revision `415a4d0` targeting Godot 4.6 stable .NET.

Objective and explicit non-goals: Port immutable canonical preset adaptation, ground-contact constraints, deterministic terrain/water/obstacle clearance, street-altitude and look geometry, chassis heading, chase framing, quintic transition interpolation, and speed-responsive field-of-view math. This chunk does not create `Camera3D` nodes, consume pointer/input events, control orbit/pan, mutate mode ownership, perform physics queries, apply camera shake, release controlled entities, or transition runtime state.

Documents and source files read: `docs/GODOT_4_6_CSHARP_PORT_PLAN.md`, the Phase 0 and prior Phase 2 handoffs, `src/camera/CameraPresets.js`, `CameraGroundConstraint.js`, `CameraClearanceQuery.js`, `StreetLevelCameraController.js`, `CameraRig.js`, their four matching test files, canonical `camera-presets.json`, and the existing typed `GameContentRegistry` camera definitions.

Files/scenes/resources added or changed: Added `MetroPulse.Domain/Camera/CameraGeometry.cs`, `CameraPresetModel.cs`, `CameraGroundConstraintModel.cs`, `CameraClearanceQuery.cs`, `StreetCameraModel.cs`, and `ChaseCameraModel.cs`; added `CameraModelTests.cs`; updated the port plan, parity matrix, and this handoff. No scene, camera node, input action, collision layer, browser source, fixture, canonical content record, or save schema changed.

Authoritative owners touched: `GameContentRegistry` remains the sole canonical preset coordinate owner. `CameraPresetModel` exposes those validated records as immutable runtime poses. `CameraGroundConstraintModel` owns surface-contact and horizontal leveling geometry. `CameraClearanceQuery` owns deterministic safe-origin inspection/search over injected immutable world snapshots. `StreetCameraModel` owns altitude hysteresis, yaw/pitch, exponential leveling, and local/macro pivot geometry. `ChaseCameraModel` owns planar heading, target-type framing, forward look-ahead, quintic interpolation, and speed-FOV math. Future Godot camera adapters consume these results and own all node mutation.

Public APIs, signals, events, input actions, collision layers: Added immutable vector/quaternion/pose, ground-result, clearance defaults/options/obstacle/inspection, and chase-request records; stable clearance reason and target-type tokens; production preset loading/query; ground constraints; clearance inspection/resolution; street altitude/rotation/level/pivot helpers; and chase heading/pose/easing/interpolation/FOV helpers. No listener, signal, timer, mutable singleton, input action, or collision layer was added.

Stable IDs/schema/content changes: Preserved all nine preset IDs and every canonical coordinate; ground clearance `0.75`, minimum look distance `5`, contact/direction epsilons; clearance defaults `0.8/0.7/2.5/30/16`; `INVALID_POSITION/TERRAIN/WATER/OBSTACLE`; street thresholds `8/10`, pivot `0.75`, pitch `−55°..65°`, leveling response `4.5`; chase distances/heights `28/9`, `15/4.5`, `17/6.5`, `8/3.5`; aircraft/vehicle look-ahead caps `14/6`; default FOV `60`, aircraft warp `+10` at `64`, and other warp `+16` at `130`. No serialized state was introduced.

Behavior implemented: Presets are immutable views of canonical content. Ground contact clamps immediately and optionally levels the target, with a stable forward fallback for vertical/degenerate views. Clearance first lifts above terrain, rejects water and sphere/AABB overlaps, honors stable ignored entity IDs, then searches deterministic rings from a preferred direction and fails closed. Street look uses local-surface hysteresis, bounded pitch, responsive yaw, frame-rate-invariant exponential leveling, and safe pivot restoration. Physics chassis quaternion heading ignores render lean. Chase poses preserve independent yaw, use target-type framing and capped forward look-ahead, interpolate with quintic smoothstep, and widen/smooth FOV from speed.

Known deviations and ADR links: No behavior deviation requires an ADR. Typed immutable vectors and stable obstacle/entity IDs replace Three.js vectors and JavaScript object-identity ignores. Invalid/nonfinite direction inputs fall back safely instead of allowing NaN propagation. The clearance query consumes injected terrain/water/obstacle snapshots; the Phase 4/5 adapter remains responsible for producing those snapshots from authoritative Godot collision owners. Shake randomness is intentionally not ported before Phase 2.6 deterministic streams.

Tests added and exact commands: Added twelve xUnit cases, increasing the domain suite from 124 to 136. Coverage mirrors the pure browser cases: all canonical presets/read-only publication, terrain clamp/level/delegation/elevated and vertical views, street altitude/yaw/level lock and frame-rate invariance, local/macro pivots, sloped-terrain clearance, deterministic obstacle search and ignored follow target, preferred water escape/fail-closed search, planar quaternion heading, independent chase yaw, aircraft framing/look-ahead/FOV, FOV smoothing, and quintic interpolation. Commands: `dotnet test godot/MetroPulse.Domain.Tests/MetroPulse.Domain.Tests.csproj --no-restore --filter FullyQualifiedName~CameraModelTests`, `dotnet test godot/MetroPulse.Domain.Tests/MetroPulse.Domain.Tests.csproj --no-restore`, `dotnet format godot/MetroPulse.Godot/MetroPulse.Godot.sln --no-restore --verify-no-changes`, `npm run baseline:check`, `npm run godot:content:check`, and `GODOT_BIN=/private/tmp/Godot46Mono/Godot_mono.app/Contents/MacOS/Godot ./godot/scripts/verify.sh`.

Test results and artifact paths: The focused camera suite passes `12/12` and the domain suite passes `136/136`. The full verification gate exits `0`: export preset identifiers validate; canonical extraction and Phase 0 baseline checks have zero drift; the deterministic .NET build completes with zero warnings and errors; Godot 4.6 stable .NET completes headless import; and all `18` foundation integration assertions pass. TRX output is `godot/artifacts/test-results/domain-tests.trx`.

Performance/resource counts before and after: Domain tests increased from 124 to 136. Runtime node/body/timer/input/collision counts remain unchanged at zero for this assembly. Preset, ground, street, chase, easing, and FOV helpers are constant-time. Clearance inspection is linear in obstacle count; resolution is bounded by configured rings × samples × obstacle count.

Manual checks performed: None; this slice has no player-facing or engine-owned surface. Camera feel, clipping against live geometry, pointer sensitivity, mode/preset handoff, shake accessibility, fixed screenshots, and node/resource ownership remain Phases 4/5/9/11 checks.

Open defects with severity and reproduction: None found in the ported surface.

Compatibility adapters and removal conditions: `CameraPresetModel.LoadProduction` bridges canonical content into immutable camera poses. Injected terrain/water/obstacle delegates remain the explicit boundary until Phase 4 world collision owners expose snapshots. Godot `CameraRig`/`CameraClearanceService` adapters must translate these records to `Vector3`/`Camera3D` without copying formulas. Browser camera modules remain compatibility evidence until live camera and transition tests pass.

Next safe task: Continue Phase 2.5 item 10 with dependency-safe pedestrian model helpers around the existing canonical archetype catalog, while keeping `CharacterBody3D`, animation, collision movement, control transfer, and spawning in Phase 5/6 adapters.

Unsafe/blocked tasks and required decision: Do not port DOM pointer binding, OrbitControls state, SceneManager mode changes, controlled-entity release, camera-node mutation, or `Math.random` shake into the domain assembly. Do not query a Godot physics world directly from domain code. No external decision blocks the pedestrian helper slice.
