# Phase 2 Outcome and Condition Kernel Handoff

Chunk ID and status: `phase-2-outcome-condition-kernel`; complete as the ninth Phase 2 slice. Phase 2.5 dependency item 7 is complete; Phase 2 overall remains in progress.

Source revision / Godot revision: Frozen browser reference `44a286a74557adfe4fabd3a6e16b9006079eba32`; implementation based on Godot revision `96f7501` targeting Godot 4.6 stable .NET.

Objective and explicit non-goals: Port the renderer-independent `MissionOutcomeService` and `CityConditionService` contracts, including atomic/idempotent cross-system consequences, explanations, persistence, and authored requirement evaluation. Complete the deferred economy mission-completion call-shape adapter. This chunk does not port mission availability/lifecycle/checkpoints/retry, incident-response scheduling, result UI, save repositories/envelopes, runtime traffic/bridge/weather owners, world mutation, or Godot nodes.

Documents and source files read: `docs/GODOT_4_6_CSHARP_PORT_PLAN.md`, `docs/MISSION_LIFECYCLE.md`, the Phase 0 and prior Phase 2 handoffs, `src/missions/MissionOutcomeService.js`, `src/missions/CityConditionService.js`, `src/missions/ContractUtils.js`, the mission-completion paths in `src/systems/EconomySystem.js`, `test/MissionOutcomeService.test.js`, `test/CityConditionService.test.js`, `test/EconomySystem.test.js`, and related persistence, traffic, service-model, incident-response, lifecycle, result-view-model, and browser tests. Phase 0 contains no dedicated outcome/condition fixture; the port mirrors the frozen source test vectors and stable schemas directly.

Files/scenes/resources added or changed: Added `MetroPulse.Domain/Missions/MissionOutcomeContracts.cs`, `MissionOutcomeService.cs`, `CityConditionContracts.cs`, and `CityConditionService.cs`; added `MissionOutcomeServiceTests.cs` and `CityConditionServiceTests.cs`; added `EconomyMissionCompletionInput` and `EconomyLedger.RecordMissionCompletion`; and updated the plan, parity matrix, and this handoff. No scene, input action, collision layer, browser source, fixture, canonical content record, or authored mission changed.

Authoritative owners touched: `MissionOutcomeService` solely owns consequence transaction identity, normalization, draft application, receipts, explanations, outcome state, revision, persistence, and subscriptions. `EconomyLedger` remains the sole Capital authority; outcome commits project the complete balance first and issue one essential ledger credit/debit only after validation. `CityConditionService` owns authored query composition and requirement evaluation while reading immutable economy/outcome snapshots and narrow provider data.

Public APIs, signals, events, input actions, collision layers: Added stable source-kind, command, repair/work-order/access/follow-up, condition-type, and operator token catalogs; typed outcome source/summary/transaction/command/state/receipt/effect/explanation/event contracts; `Apply`, `HasApplied`, `GetReceipt`, `Explain`, `Snapshot`, `Serialize`, `Restore`, `ValidateState`, `CreateEmptyState`, subscribe, and destroy operations; condition query/request/result/source/context/requirement/evaluation contracts; built-in traffic, bridge, service, safety, repair, land-value, weather, district, and flag queries; custom resolver registration; path/operator evaluation; and ALL/ANY aggregation. No engine signal, input action, or collision layer was added.

Stable IDs/schema/content changes: Preserved outcome state version `1`, receipt version `1`, caller transaction IDs, default `command-<1-based index>` IDs, all 14 uppercase command tokens, source/follow-up/access/repair/work-order tokens, all nine condition types, and all ten condition operators. Transaction fingerprints use recursively key-sorted camel-case JSON and are validated during restore. C# wraps outcome dictionaries under the typed snapshot `State` property rather than spreading them onto an untyped object; the serialized state document retains `version`, `sequence`, `state`, and `transactions`. No save envelope or content ID changed.

Behavior implemented: The entire transaction and every authored ID/range are normalized before mutation; commands apply to a draft; projected Capital cannot cross below zero; only then does one ledger credit/debit occur and the draft/receipt commit. Duplicate identical transactions return the prior receipt with `Duplicate=true`; reused IDs with different content conflict; an active-commit guard blocks observer replay; listener failures are isolated. Commands cover Capital, buildings, infrastructure, incidents/resolution, repairs, service outages, traffic, bounded faction reputation, progression, unlocks, news, follow-up missions, and scalar authored flags. Receipts preserve before/after values and per-command explanations. Restore validates the complete candidate, fingerprint and sequence uniqueness, command/effect counts, typed stored state, and authored references before replacing authority, without replaying Capital; restored receipt collections are defensively copied.

Condition behavior implemented: Traffic merges live metrics and authored policies without double-applying providers that already include those policies. Bridge queries combine base state, infrastructure, repair, and traffic. Service outages are district-aware; safety combines fire coverage, active incident severity, and traffic hazard; repair defaults are explicit; land value combines economy breakdowns with authored incident modifiers; weather exposes the frozen clear/mist/rain/storm profiles; districts combine release scope, economy unlocks, authored overrides, and incident disruption; flags preserve scalar values. Requirements support nested paths, equality/inequality, numeric comparisons, membership, containment, truthy/falsy checks, and deterministic ALL/ANY evaluation. Built-in resolver ownership cannot be replaced.

Known deviations and ADR links: No behavior deviation requires an ADR. Typed C# commands replace untyped JavaScript objects, and typed snapshots keep outcome dictionaries under `State`; serialized names/tokens and semantic content remain stable. Provider callbacks expose typed traffic, bridge, and weather records instead of duck-typed objects. The browser permits a single non-array interaction candidate, which is unrelated to this slice and remains covered by the typed interaction contract handoff.

Tests added and exact commands: Added eight outcome/economy-adapter cases and six condition cases, increasing the domain suite from 76 to 90. Coverage includes every consequence family, explanation values, duplicate/conflict behavior, observer reentrancy, late-command and affordability atomic failure, authored-ID and duplicate-command rejection, incident resolution, faction bounds, JSON persistence, no Capital replay, atomic corrupt-state rejection, restored collection ownership, mission-completion adapter dedupe, live/authored traffic and bridge composition, district-aware service/safety/repair, land/weather/district/flag values, all/path/any operators, custom resolver ownership, and invalid requests. Commands: `dotnet build godot/MetroPulse.Domain.Tests/MetroPulse.Domain.Tests.csproj --no-restore --verbosity:minimal`, `./godot/scripts/test-domain.sh`, `dotnet format godot/MetroPulse.Godot/MetroPulse.Godot.sln --no-restore --verify-no-changes`, `npm run baseline:check`, `npm run godot:content:check`, and `GODOT_BIN=/private/tmp/Godot46Mono/Godot_mono.app/Contents/MacOS/Godot ./godot/scripts/verify.sh`.

Test results and artifact paths: The full verification gate exits `0`: export preset identifiers validate; canonical extraction and Phase 0 baseline checks have zero drift; the deterministic .NET build completes with zero warnings and errors; the domain suite passes `90/90`; Godot 4.6 stable .NET completes headless import; and all `18` foundation integration assertions pass. TRX output is `godot/artifacts/test-results/domain-tests.trx`.

Performance/resource counts before and after: Domain tests increased from 76 to 90. Runtime node/body/timer/input/collision counts remain unchanged at zero for this assembly. Outcome application is one draft copy plus a bounded command scan; condition queries scan small in-memory outcome maps. Snapshots, receipt arrays, state maps, source lists, provider parameters, and restored collections are copied into read-only ownership boundaries.

Manual checks performed: None; this slice has no player-facing or engine-owned surface. Manual mission/result/world acceptance remains with Phases 8 and 9.

Open defects with severity and reproduction: None found in the ported surface.

Compatibility adapters and removal conditions: `EconomyLedger.RecordMissionCompletion` preserves the existing mission-owner call shape and duplicate protection until all mission completion flows exclusively publish stable outcome transactions; remove it only after lifecycle integration proves no caller remains. Typed traffic, bridge, and weather callbacks remain composition adapters until Phase 2.5 item 10 supplies their pure owners; their consumers must not recreate outcome composition. The Phase 3 save envelope may embed the version-1 outcome document but must not replay its Capital effects during restore.

Next safe task: Begin Phase 2.5 item 8 with pure mission availability and lifecycle phase ownership, then add weather policy, checkpoint, failure, retry, cleanup, and exactly-once outcome handoff in dependency order. Keep runtime entity spawning, UI, input, and Godot scenes outside the domain assembly.

Unsafe/blocked tasks and required decision: Do not port the runtime incident-response scheduler or `ServiceWorkInteractionProvider` until item 8 defines cleanup/outcome ownership and item 10 provides the required world/traffic helpers. Do not bind condition callbacks to temporary Godot nodes. Neither deferral requires a product decision.
