# Phase 2 Canonical Content Handoff

Chunk ID and status: `phase-2-canonical-content`; Complete as the second Phase 2 slice. Phase 2 overall remains in progress.

Source revision / Godot revision: Frozen browser reference `44a286a74557adfe4fabd3a6e16b9006079eba32`; implementation based on Godot revision `14a7bb958526` targeting Godot 4.6 stable .NET.

Objective and explicit non-goals: Extract building, construction-vocabulary, weather, and mission-weather-policy authorities into deterministic canonical JSON; prove building/weather record and scalar parity against Phase 0; load and validate those artifacts through one immutable C# production registry. This chunk does not port economy balance, vehicle/camera/world-layout/archetype catalogs, weather clock behavior, mission availability/lifecycle, rendering, physics, boot wiring, persistence, or UI.

Documents and source files read: `docs/GODOT_4_6_CSHARP_PORT_PLAN.md`, `docs/DATA_VALIDATION.md`, `docs/MVP_SCOPE.md`, `docs/port_handoffs/phase-2-content-state-kernel.md`, `src/world/BuildingCatalog.js`, `src/world/ConstructionVocabulary.js`, `src/systems/Weather.js`, `src/missions/MissionPolicyDefinitions.js`, `src/data/GameDataValidator.js`, `Tools/BaselineCapture/baseline-fixtures.mjs`, `test/fixtures/godot-port/phase0/content-registry.json`, `.github/workflows/ci.yml`, and the Godot build/test scripts.

Files/scenes/resources added or changed: Added `Tools/GodotContentExtraction/extract-content.mjs`; canonical `buildings.json`, `weather.json`, and `mission-weather-policies.json` under `godot/MetroPulse.Domain/Content/Data`; typed models, loader, validator, and `GameContentRegistry`; `CanonicalContentTests`; `godot/scripts/validate-content-extraction.sh`; package scripts; CI and full verification integration; the port-plan status, parity matrix, and this handoff. `MissionRegistry` now validates weather-policy references against the canonical policy registry. No browser authority, authored scalar, scene, input action, collision layer, or save schema changed.

Authoritative owners touched: The frozen JS modules remain extraction inputs during the dual-engine port. The committed generated JSON files are the C# compatibility artifacts. `CanonicalContentLoader` owns strict JSON deserialization; `CanonicalContentValidator` owns building/weather/policy shape, enum, range, reference, version, and source-revision validation; `GameContentRegistry` owns immutable publication and lookups; `MissionRegistry` owns mission-to-policy reference validation.

Public APIs, signals, events, input actions, collision layers: Added `CanonicalContentLoader.LoadBuildings`, `LoadWeather`, and `LoadMissionWeatherPolicies`; `CanonicalContentValidator.ValidateBuildings`, `ValidateWeather`, and `ValidateMissionWeatherPolicies`; `GameContentRegistry.LoadProduction`, `GetBuilding`, and `GetWeather`; typed catalog documents/records; and `ContentCounts`. Added no signals, events, input actions, or collision changes.

Stable IDs/schema/content changes: No stable content ID or authored value changed. Canonical artifact schema version is 1 and pins source revision `44a286a74557adfe4fabd3a6e16b9006079eba32`. The production registry contains 19 buildings, 4 weather modes, 5 mission-weather policies, 15 missions, 47 dialogue nodes, 7 zones, 4 districts, 4 factions, and 3 progression tiers. Generated artifact SHA-256 values are `a750b5257b982009454dffb2de6e46121af880e8326dd3cd661ee45cffb9004e` (`buildings.json`), `aedefc311f99cbc753554b3bb5defb6d3c2aae674a1c0fc3c18a7891bb637556` (`mission-weather-policies.json`), and `2babf5017546ef6aa6f37f3b4c02a1c7e6d4e948d89aad95176fc8d1fa2f1047` (`weather.json`).

Behavior implemented: Extraction imports the renderer-free browser authorities, checks exact building/weather counts and ordered IDs, deep-compares every scalar to `content-registry.json`, and emits stable key-sorted JSON. Check mode byte-compares committed output and fails on drift. C# loading rejects unknown JSON properties and malformed documents. Validation fails closed on unsupported schema/source revision, duplicate IDs, invalid categories/stages/tiers/generators/road types, invalid building geometry/economic/service scalars, unsafe weather ranges, missing/invalid weather sequences/defaults, invalid policy dispositions/multipliers, unknown weather modes, and mission references to unknown policy IDs. Publication occurs only after the whole production graph validates and exposes frozen/read-only collections.

Known deviations and ADR links: None. `DD-016` remains the platform authority. The extractor records the frozen browser revision intentionally; changing that revision or an extracted scalar requires refreshed parity evidence rather than silently regenerating output.

Tests added and exact commands: Added five xUnit cases in `CanonicalContentTests` (25 total domain cases). Commands: `npm run godot:content:extract`; `npm run godot:content:check`; `npm run baseline:check`; `dotnet format godot/MetroPulse.Godot/MetroPulse.Godot.sln --verify-no-changes --no-restore`; `./godot/scripts/test-domain.sh`; and `GODOT_BIN=/private/tmp/Godot46Mono/Godot_mono.app/Contents/MacOS/Godot ./godot/scripts/verify.sh`.

Test results and artifact paths: Extraction write/check passed with zero mismatches. Phase 0 baseline check passed with zero mismatches. The full Godot verification script passed: export preset validation, extraction drift validation, deterministic build with zero warnings/errors, 25/25 domain tests in 53 ms, Godot 4.6 headless import, and 18/18 foundation integration assertions. TRX and raw Godot logs remain under `godot/artifacts/test-results/`.

Performance/resource counts before and after: Pure startup-domain data only; no nodes, bodies, timers, subscriptions, or frame work were added. Embedded canonical JSON totals 648 lines across three files. The registry parses and freezes 19 building records, 4 weather records, and 5 policy records on construction; caching/boot ownership remains Phase 3 work.

Manual checks performed: Reviewed generated JSON, record order, stable IDs, every-scalar typed round trip, extraction hashes, immutable publication behavior, error paths, embedded resource names, CI ordering, and the final authored-source diff. No interactive/player-visible behavior exists in this slice.

Open defects with severity and reproduction: None found in the completed slice. Phase 2 remains incomplete because economy balance, vehicle profiles, camera presets, countryside plan, bridge/street-furniture layout, archetypes, mutable state/coordinator, scheduler, economy, alerts, interactions, mission outcome/lifecycle, settings/bindings, deterministic streams, and pure helper families are not yet ported.

Compatibility adapters and removal conditions: `extract-content.mjs` and its generated JSON are owned by the Phase 2 content pipeline. They remain dual-engine compatibility adapters until the browser reference is retired and the JSON files become the reviewed primary authorities; removal requires byte/scalar parity evidence and updates to both build embedding and CI. The source-revision pin may change only with refreshed Phase 0-equivalent fixtures and an accepted handoff/ADR when behavior changes.

Next safe task: Extend the same extraction format to economy balance, vehicle profiles, camera presets, countryside/bridge/street-furniture layouts, and pedestrian/agent archetypes, with per-family fixture parity. After those engine-neutral catalogs are canonical, continue Phase 2.5 with pure pause/transition-coordinator contracts and scheduler math.

Unsafe/blocked tasks and required decision: Do not wire `GameContentRegistry` into the Godot boot shell before Phase 2 completes its remaining engine-neutral authorities and Phase 3 defines boot failure presentation. Do not regenerate canonical artifacts to accept drift without updating frozen evidence. No external decision currently blocks the next task.
