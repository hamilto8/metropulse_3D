# Phase 3 Boot and Capability Shell Handoff

Chunk ID and status: `phase-3-boot-capability-shell`; complete as the first Phase 3 slice. Phase 3 remains in progress.

Source revision / Godot revision: Frozen browser reference `44a286a74557adfe4fabd3a6e16b9006079eba32`; implementation starts from Godot revision `1be5d32` and targets Godot 4.6 stable .NET.

Objective and explicit non-goals: Replace immediate native session construction with a fail-closed, observable boot pipeline; run real desktop capability and content checks; keep the empty session non-interactive until final readiness. This chunk does not implement settings storage, InputMap actions, save discovery/application, the rotating file repository, migrations, deferred restore, browser import, action-selection UI, or the complete Phase 3 diagnostics snapshot.

Documents and source files read: The complete port plan invariants and Phase 3 section; Phase 2 exit audit; `SETTINGS_AND_BINDINGS_STORE.md`; `VERSIONED_SAVE_SERVICE.md`; browser `BootPipeline.js`, `CapabilityChecker.js`, `SaveDiscovery.js`, `SettingsBootstrap.js`, and `BootFlow.test.js`; the native composition root, session shell, boot presenter, diagnostics overlay/runner, scenes, project configuration, content registry/validators, verification scripts, and parity matrix.

Files/scenes/resources added or changed: Added domain boot/capability contracts and five unit cases, plus the Godot `DesktopCapabilityChecker`. Reworked `CompositionRoot`, `SessionShell`, `BootStatusPresenter`, `IntegrationTestRunner`, and the boot input blocker in `Main.tscn`; updated the Godot informational version, port plan, parity matrix, and this handoff.

Authoritative owners touched: `BootPipeline` owns ordered, non-reentrant stage execution and immutable prior/final result maps. `DesktopCapabilityReport` owns capability compatibility and actionable failure composition. `DesktopCapabilityChecker` is the Godot-only probe adapter. `CompositionRoot` owns stage wiring and session lifetime. `SessionShell` alone records whether interactive control has been released.

Public APIs, signals, events, input actions, collision layers: Added stable boot stage IDs, `BootPipeline.RunAsync`, boot progress records, `DesktopCapabilityReport.AssertCompatible`, `CompositionRoot` boot result/report properties, and `SessionShell.ReleaseInteractiveControl`. Structured log events use `boot.<stage-id>.running|complete|failed`, `phase3.boot.ready`, and `phase3.boot.failed`. No Godot signal, InputMap action, collision layer, or autoload was added.

Stable IDs/schema/content changes: Added boot stage IDs `capability-checks`, `settings-bootstrap`, `content-validation`, `save-discovery`, `action-selection`, `session-construction`, `save-application`, `final-readiness`, and `interactive-release`; capability IDs are `user-data`, `graphics-backend`, `input-service`, and `project-resources`. No save schema, content record, feature flag, mission ID, or collision identity changed.

Behavior implemented: Boot currently runs six stages in order: desktop capabilities, canonical content, explicit temporary `NEW_GAME` selection, disabled empty-session construction, readiness validation, and interactive release. The capability adapter performs a real uniquely named `user://` write, flush, close, exact readback, and cleanup; accepts the headless renderer only in automated mode; checks Godot input service availability; and validates the configured packaged SessionRoot. Any stage failure stops later stages, disposes a constructed session, retains the blocking boot layer, and presents an error code plus remedies. The domain pipeline rejects duplicate/missing stages and concurrent execution and may be retried after failure.

Known deviations and ADR links: No parity deviation requires an ADR. Until save discovery and the boot action presenter land, the action-selection stage deliberately returns `NEW_GAME`; this is named temporary behavior and does not claim Continue/Recover parity. Physical input-device and InputMap validation remain deferred to the input-adapter chunk, while this slice checks only Godot input-service availability. Save/content-version compatibility remains split: canonical content validates now; save versions have no owner until the persistence chunk.

Tests added and exact commands: Added `BootPipelineTests` with order/immutability, fail-closed/retry, concurrency, stage validation, and actionable capability cases. Expanded native integration from 18 to 28 assertions. Commands run: `dotnet test godot/MetroPulse.Domain.Tests/MetroPulse.Domain.Tests.csproj --no-restore --filter FullyQualifiedName~BootPipelineTests`; `dotnet build godot/MetroPulse.Godot/MetroPulse.Godot.csproj --no-restore -v:minimal`; `dotnet format godot/MetroPulse.Godot/MetroPulse.Godot.sln --no-restore --verify-no-changes`; `GODOT_BIN=/Applications/Godot_mono.app/Contents/MacOS/Godot ./godot/scripts/test-integration.sh`; `GODOT_BIN=/Applications/Godot_mono.app/Contents/MacOS/Godot ./godot/scripts/verify.sh`; and `npm test`.

Test results and artifact paths: Focused boot tests pass 5/5; the browser reference suite remains 389/389; the full domain suite passes 185/185; the native build succeeds with zero warnings/errors; format verification passes; and all 28 headless integration assertions pass. The complete `godot/scripts/verify.sh` gate exits 0, including export-preset validation, zero canonical-content drift, 62-file Phase 2 ownership coverage across 71 domain files, build, tests, headless import, and integration. Domain TRX output is `godot/artifacts/test-results/domain-tests.trx`; headless import and integration logs are `godot/artifacts/test-results/godot-import.log` and `godot/artifacts/test-results/godot-integration.log`.

Performance/resource counts before and after: Pure domain tests increase from 180 to 185. Initial headless boot stages increase from one monolithic try block to six explicit stages and integration assertions from 18 to 28. One session is constructed only after pre-session checks and is disposed once. The temporary capability file is closed and deleted within its check. No physics body, timer, audio player, gameplay process, collision layer, or persistent node is added.

Manual checks performed: Reviewed the structured headless log to confirm stage order, SessionRoot construction only after capability/content/action checks, final readiness before interactive release, and exactly one session-disposal event. No player-facing interactive manual test applies yet because action selection remains intentionally non-interactive.

Open defects with severity and reproduction: None found in this slice. The broader Phase 3 exit scenarios (Continue, Recover, corrupt/future saves, settings restart, write fault injection, and browser-save import) are unimplemented work, not regressions in this chunk.

Compatibility adapters and removal conditions: Browser boot/capability modules remain the frozen reference. Remove their compatibility ownership only after all nine Godot stage IDs have implementations and the Phase 3 exit scenarios pass. The temporary `NEW_GAME` selector must be replaced, not bypassed, by the save-discovery/action-selection presenter. The desktop capability adapter remains the target implementation.

Next safe task: Port the validated settings document into a `user://` repository, add the InputMap adapter with the seven existing contexts, and insert the `settings-bootstrap` stage without changing the boot authority.

Unsafe/blocked tasks and required decision: Do not add raw key comparisons, make settings a Godot singleton, construct a session before the capability/content/save gates, claim Continue/Recover, or serialize nodes. No external decision blocks the next settings/InputMap slice.
