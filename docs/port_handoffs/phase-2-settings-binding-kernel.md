# Phase 2 Settings and Binding Kernel Handoff

Chunk ID and status: `phase-2-settings-binding-kernel`; complete as the eleventh Phase 2 slice. Phase 2.5 dependency item 9 is complete; Phase 2 overall remains in progress.

Source revision / Godot revision: Frozen browser reference `44a286a74557adfe4fabd3a6e16b9006079eba32`; implementation based on Godot revision `aa279f0` targeting Godot 4.6 stable .NET.

Objective and explicit non-goals: Port the renderer-independent settings and keyboard/mouse binding authority, including exact defaults, version-1 migration, version-2 validation, accessibility preference ranges/tokens, contextual action maps, binding compatibility/conflicts, immutable snapshots, atomic injected storage, reset scopes, and observer isolation. This chunk does not apply preferences to cameras, audio, rendering, UI themes, mission timers, or controls; translate inputs into Godot `InputMap`; capture live input; render settings UI; or choose platform-specific storage.

Documents and source files read: `docs/GODOT_4_6_CSHARP_PORT_PLAN.md`, `docs/SETTINGS_AND_BINDINGS_STORE.md`, the Phase 0 and prior Phase 2 handoffs, `src/settings/SettingsSchema.js`, `src/settings/SettingsStore.js`, `src/settings/SettingsRuntime.js`, `src/boot/SettingsBootstrap.js`, `src/systems/ControlBindings.js`, the binding paths in `src/systems/InputManager.js`, `test/SettingsStore.test.js`, `test/InputManager.test.js`, and `test/fixtures/godot-port/phase0/settings-bindings-alerts.json`.

Files/scenes/resources added or changed: Added `MetroPulse.Domain/Settings/SettingsContracts.cs`, `ControlBindingCatalog.cs`, `SettingsValidator.cs`, and `SettingsStore.cs`; added `SettingsStoreTests.cs`; and updated the port plan, parity matrix, and this handoff. The existing Phase 0 settings/bindings/alerts fixture remains unchanged and was already embedded by the alert slice. No scene, input action, `InputMap`, collision layer, browser source, fixture, canonical content record, authored mission, or save envelope changed.

Authoritative owners touched: `SettingsValidator` owns default preferences, document version/migration, complete range/type/enum validation, and binding composition. `ControlBindingCatalog` owns the seven stable contexts, 45 contextual action entries, 30 distinct actions, default slots, accepted input syntax, device compatibility, conflict rules, reserved browser inputs, and prompt labels. `SettingsStore` owns the live validated document, injected persistence transaction boundary, mutation/reset operations, and observers. Future Godot adapters consume snapshots/events and must not maintain a second settings truth.

Public APIs, signals, events, input actions, collision layers: Added stable preference token, input-interface, context, and input-code catalogs; typed nested preferences, document, conflicts, reserved issues, load result, event, and storage-port contracts; validation/default creation; binding lookup/label/conflict/reserved/override validation; store load/snapshot/get/set/replace, binding edit/reset, subscribe/destroy, and default/effective lookup operations. No engine signal, Godot input action, or collision change was added.

Stable IDs/schema/content changes: Preserved storage key `metropulse3d:settings:v1`, document version `2`, version-1 `{ reducedMotion, textScale }` migration, every default scalar/boolean/token, seven uppercase context IDs, 45 context/action records, 30 action IDs, directional slot order, accepted keyboard/mouse code grammar, eight browser-reserved F-keys, and the Management left-click orbit/select exception. Serialized property names remain camel case. Gamepad mappings remain fixed browser evidence and are not mutable domain state. No save-envelope version changed.

Behavior implemented: Validation requires a complete version-2 preference graph and rejects missing objects, nonfinite/out-of-range values, wrong types, unknown tokens, unknown contexts/actions, empty/oversized/duplicate input arrays, malformed codes, keyboard bindings on mouse-only actions, mouse inputs on keyboard actions, pointer-source edits, contextual conflicts, and reserved browser inputs. Version 1 clamps text scale, carries supported reduced-motion values, and fills all other defaults. Mutations validate a whole candidate and serialize to injected storage before replacing live state; storage failure cannot mutate state or emit an event. Snapshots and nested maps/lists are read-only. Context, binding-only, and all-settings resets preserve exact defaults. Listener failures are reported through the optional callback and cannot interrupt healthy observers.

Known deviations and ADR links: No behavior deviation requires an ADR. C# uses typed immutable preference records and an `ISettingsStorage` port instead of untyped JavaScript objects and direct LocalStorage access. Standard .NET exception types add parameter metadata to some messages; the stable blocker text and validation outcome remain exact. The browser's DOM/Three.js `SettingsRuntime` and legacy-key synchronization are intentionally deferred to their Godot camera/audio/render/UI and `InputMap` adapters.

Tests added and exact commands: Added eight xUnit cases, increasing the domain suite from 98 to 106. Coverage includes exact Phase 0 default/customized JSON, prompt label and reserved-key evidence, version-1 migration/persistence, read-only publication, partial/future/range rejection, atomic observable updates and reload, storage/listener failure isolation, conflict/reserved/device rejection, context/global resets, and directional-slot persistence. Commands: `dotnet test godot/MetroPulse.Domain.Tests/MetroPulse.Domain.Tests.csproj --no-restore --filter FullyQualifiedName~SettingsStoreTests`, `dotnet test godot/MetroPulse.Domain.Tests/MetroPulse.Domain.Tests.csproj --no-restore`, `dotnet format godot/MetroPulse.Godot/MetroPulse.Godot.sln --no-restore --verify-no-changes`, `npm run baseline:check`, `npm run godot:content:check`, and `GODOT_BIN=/private/tmp/Godot46Mono/Godot_mono.app/Contents/MacOS/Godot ./godot/scripts/verify.sh`.

Test results and artifact paths: The focused settings suite passes `8/8` and the domain suite passes `106/106`. The full verification gate exits `0`: export preset identifiers validate; canonical extraction and Phase 0 baseline checks have zero drift; the deterministic .NET build completes with zero warnings and errors; Godot 4.6 stable .NET completes headless import; and all `18` foundation integration assertions pass. TRX output is `godot/artifacts/test-results/domain-tests.trx`.

Performance/resource counts before and after: Domain tests increased from 98 to 106. Runtime node/body/timer/input/collision counts remain unchanged at zero for this assembly. Validation scans a fixed-size preference graph and at most 45 effective action lists; commits serialize one small document and copy bounded maps/lists into read-only ownership.

Manual checks performed: None; this slice has no player-facing or engine-owned surface. Manual preference application, input capture/remapping, focus, prompt, restart persistence, and accessibility acceptance remain Phases 3 and 9.

Open defects with severity and reproduction: None found in the ported surface.

Compatibility adapters and removal conditions: `ISettingsStorage` is the persistence seam for the Phase 3 platform repository; test memory storage is not production code. The future Godot adapter must translate stable context/action slots into `InputMap`, quarantine held inputs during changes, and refresh prompts without rewriting validation. Browser-reserved keys remain rejected for frozen parity until an approved platform-specific policy/ADR replaces that rule. Settings save-envelope restoration must call `Replace` so invalid or partial state cannot bypass validation.

Next safe task: Begin Phase 2.5 item 10 with pure traffic, pedestrian, camera, weather, time, placement, and flight-model helpers in dependency-sized slices. Use existing canonical profiles and immutable economy/outcome snapshots; keep nodes, physics bodies, rendering, audio, input, and scene ownership outside the domain assembly.

Unsafe/blocked tasks and required decision: Do not bind settings directly to Godot `InputMap`, cameras, audio buses, render effects, UI controls, or the Phase 3 save repository before those adapters own their transaction and recovery behavior. Do not add raw key comparisons or a second mutable settings copy. No external decision blocks item 10.
