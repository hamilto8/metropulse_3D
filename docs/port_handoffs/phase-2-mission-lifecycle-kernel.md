# Phase 2 Mission Lifecycle Kernel Handoff

Chunk ID and status: `phase-2-mission-lifecycle-kernel`; complete as the tenth Phase 2 slice. Phase 2.5 dependency item 8 is complete; Phase 2 overall remains in progress.

Source revision / Godot revision: Frozen browser reference `44a286a74557adfe4fabd3a6e16b9006079eba32`; implementation based on Godot revision `be760bc` targeting Godot 4.6 stable .NET.

Objective and explicit non-goals: Port the renderer-independent mission availability and lifecycle authority, including authored prerequisites, outcome follow-up state, repeatability, canonical weather disposition, phase/run ownership, checkpoints, success/failure resolution, cleanup identity, receipt-gated completion, bounded retry, recovery, save gating, dialogue progress, and versioned restoration. This chunk does not spawn or remove runtime entities, drive vehicles/routes, render offer/HUD/result UI, bind input, create Godot nodes, or implement the Phase 3 save repository.

Documents and source files read: `docs/GODOT_4_6_CSHARP_PORT_PLAN.md`, `docs/MISSION_LIFECYCLE.md`, the Phase 0 and prior Phase 2 handoffs, `src/missions/MissionLifecycleController.js`, `src/missions/MissionPolicyDefinitions.js`, `src/missions/MissionOutcomeService.js`, `src/missions/CityConditionService.js`, `test/MissionLifecycleController.test.js`, canonical mission-weather policy data, and the existing C# mission, outcome, condition, economy, content-registry, and persistence contracts.

Files/scenes/resources added or changed: Added `MetroPulse.Domain/Missions/MissionLifecycleContracts.cs` and `MissionLifecycleController.cs`; added `MissionLifecycleControllerTests.cs`; extended the typed mission definition with nullable `repeatable`; and updated the port plan, parity matrix, and this handoff. No scene, input action, collision layer, browser source, fixture, canonical content record, authored mission, or save envelope changed.

Authoritative owners touched: `MissionLifecycleController` solely owns mission selection, lifecycle phase, active run/attempt, checkpoint, resolution, cleanup transaction identity, receipt attachment, completed IDs, run counts, chronology step, dialogue history, recovery, and mission save gating. `MissionOutcomeService` remains the sole consequence/receipt authority; `EconomyLedger` remains the sole Capital authority; `CityConditionService` remains the authored city-query authority; `GameContentRegistry` remains the canonical weather-policy authority.

Public APIs, signals, events, input actions, collision layers: Added stable phase, availability, weather-disposition, retry-strategy, and prerequisite token catalogs; typed weather, availability, progress, checkpoint, resolution, run, state, event, save, retry, and recovery contracts; `EvaluateAvailability`, `Prepare`, `BeginBriefing`, `AbandonBriefing`, `Accept`, `BeginExecution`, checkpoint/resume, success/failure resolution, cleanup transaction creation/commit/failure, retry decision, begin/finish recovery, save decision, dialogue recording, snapshot/progress/serialize/restore, validation, subscribe/destroy, and static weather evaluation. Events are typed C# observer callbacks with isolated listener failures. No engine signal, input action, or collision change was added.

Stable IDs/schema/content changes: Preserved lifecycle state version `1`; all 11 uppercase phase tokens; four weather dispositions; four availability states; three retry strategies; three prerequisite types; `mission:<runId>:attempt-<attempt>:<outcome>` cleanup IDs; `<missionId>:run-<number>` run IDs; and browser-compatible `[missionId,count]` run-count JSON. Weather uses the unchanged five canonical policies. Success alone advances completed IDs/chronology. No content ID or save envelope changed.

Behavior implemented: Availability composes completed-mission, follow-up-status, city-condition, nonrepeatable completion, and current-weather evidence with stable reasons and fail-closed unavailable mission data. Acceptance rechecks availability, applies weather time/reward multipliers with JavaScript-compatible nonnegative rounding, and selects objective-default or authored retry policy. The controller owns the exact preparation, briefing, approach, active, checkpoint, completion/failure, cleanup, result, recovery, and idle transitions. Checkpoints can only be recorded during active execution. Cleanup receives one deterministic identity per run attempt, requires a matching outcome receipt before exposing result, and remains mission-critical/save-blocked after storage failure. Failed results choose last-checkpoint or approach restart by objective, enforce maximum attempts, clear prior resolution/receipt identity on retry, and allocate a new attempt transaction. Restore validates the whole candidate before replacement and publishes copied/read-only collections and cloned JSON payloads.

Known deviations and ADR links: No behavior deviation requires an ADR. Typed C# method parameters replace JavaScript option bags, and lifecycle JSON uses the repository's shared camel-case serializer options. Receipt commit accepts the typed `MissionOutcomeReceipt` rather than a duck-typed object. These differences narrow invalid runtime shapes without changing lifecycle tokens, identities, or outcomes.

Tests added and exact commands: Added eight xUnit cases, increasing the domain suite from 90 to 98. Coverage mirrors every frozen browser lifecycle case: composed prerequisites, complete success phase sequence and ownership, idempotent cleanup identity, race/sabotage checkpoint retry versus delivery restart, bounded attempts and distinct transaction IDs, allowed/adapted/delayed/blocked weather, checkpoint JSON round-trip and collection ownership, and mission-critical cleanup failure/save blocking. Commands: `dotnet test godot/MetroPulse.Domain.Tests/MetroPulse.Domain.Tests.csproj --no-restore --filter FullyQualifiedName~MissionLifecycleControllerTests`, `dotnet test godot/MetroPulse.Domain.Tests/MetroPulse.Domain.Tests.csproj --no-restore`, `dotnet format godot/MetroPulse.Godot/MetroPulse.Godot.sln --no-restore --verify-no-changes`, `npm run baseline:check`, `npm run godot:content:check`, and `GODOT_BIN=/private/tmp/Godot46Mono/Godot_mono.app/Contents/MacOS/Godot ./godot/scripts/verify.sh`.

Test results and artifact paths: The focused lifecycle suite passes `8/8` and the domain suite passes `98/98`. The full verification gate exits `0`: export preset identifiers validate; canonical extraction and Phase 0 baseline checks have zero drift; the deterministic .NET build completes with zero warnings and errors; Godot 4.6 stable .NET completes headless import; and all `18` foundation integration assertions pass. TRX output is `godot/artifacts/test-results/domain-tests.trx`.

Performance/resource counts before and after: Domain tests increased from 90 to 98. Runtime node/body/timer/input/collision counts remain unchanged at zero for this assembly. Availability is bounded by one mission's prerequisite count plus small outcome maps; restore and transition snapshots copy bounded mission progress, receipt arrays, and JSON payloads into read-only ownership boundaries.

Manual checks performed: None; this slice has no player-facing or engine-owned surface. Manual offer, route, checkpoint placement, result, cleanup, and recovery acceptance remains Phase 8.

Open defects with severity and reproduction: None found in the ported surface.

Compatibility adapters and removal conditions: The lifecycle emits typed `MissionOutcomeTransaction` values into the existing outcome authority; `EconomyLedger.RecordMissionCompletion` remains only for callers not yet migrated and may be removed after all Phase 8 completion paths use lifecycle cleanup receipts. The weather callback is a narrow compatibility adapter until item 10 supplies the pure weather/time owner. The Phase 3 save envelope may embed the version-1 lifecycle state but must obey `CanSave` and restore it without replaying outcome receipts.

Next safe task: Begin Phase 2.5 item 9 with pure settings and binding validation against the unchanged Phase 0 settings/bindings/alerts fixture. Preserve stable action IDs and serialized camel-case names; keep Godot `InputMap`, UI controls, render/audio adapters, and platform-specific application outside the domain assembly.

Unsafe/blocked tasks and required decision: Do not bind lifecycle transitions to Godot scenes, runtime entity cleanup, input, HUD/result presentation, or the Phase 3 save repository before those phase owners exist. Do not let runtime adapters mint cleanup identities or apply outcome commands directly. No external decision blocks item 9.
