# Phase 2 City Pulse Kernel Handoff

Chunk ID and status: `phase-2-city-pulse-kernel`; complete as the seventh Phase 2 slice. Phase 2.5 dependency item 5 is complete; Phase 2 overall remains in progress.

Source revision / Godot revision: Frozen browser reference `44a286a74557adfe4fabd3a6e16b9006079eba32`; implementation based on Godot revision `aeae301` targeting Godot 4.6 stable .NET.

Objective and non-goals: Complete the renderer-independent `EconomySystem` authority after the transactional ledger slice by porting aggregate services and productivity, demographics, happiness and demand, parcel and aggregate land value, mobility feedback, zones, incidents, district unlocks, fiscal explanations, and versioned persistence. This chunk does not port alerts or interaction priority, traffic's derivation of mobility feedback, mission outcome adapters, UI, persistence repositories, or Godot nodes.

Authorities and evidence: Read all remaining pure state and projection paths in `src/systems/EconomySystem.js`, `ConstructionVocabulary.js`, `EconomySystem.test.js`, `EconomyRecovery.test.js`, and `TrafficProductivityModel.test.js`, plus the unchanged Phase 0 `economy.json`. The C# tests mirror the source contracts and compare the newly owned frozen budget, demographic, demand, happiness, mobility, City Pulse, service, building, mission, incident, zone, district, and unlock projections at all four balanced-session horizons exactly.

Files and ownership: Added `MetroPulse.Domain/Economy/EconomyCityPulse.cs` and `MetroPulse.Domain.Tests/Economy/EconomyCityPulseTests.cs`; expanded `EconomyLedger.cs` and the frozen assertions in `EconomyLedgerTests.cs`. `EconomyLedger` remains the sole mutation/revision authority, and `EconomyBalanceDefinition` remains the sole authored balance authority. The domain assembly remains free of Godot references.

Public contracts: Added service/district tokens; typed positions, base/building/aggregate services, full building records, zones, incidents, mobility feedback, demographics, demand and happiness breakdowns, City Pulse and parcel land-value snapshots, districts, and versioned `EconomyLedgerState`. `EconomyLedger` now exposes service, mobility, spatial land-value, incident, zone, City Pulse, district, serialization, and restoration operations in addition to its existing transactions.

Behavior: Power and water coverage reduce gross productivity; fire and utility coverage reduce happiness and land value; building capacity drives population, housing, jobs, services, revenue, upkeep, amenities, and value; labor and mobility produce explainable employment/access/satisfaction results; zones replace by stable ID; active incidents affect happiness/value and apply reputation once; parcel amenities and Mayhem use linear distance falloff; district aliases share one unlock authority. Every change still publishes one before/after revision event. Persistence validates the complete candidate before mutation, round-trips through JSON, restores recovery without duplicate assistance, and intentionally resets mobility because traffic owns that derived input.

Tests and results: Added nine xUnit cases, bringing the domain suite to 63. They cover exact labor formulas and JavaScript rounding, building/service aggregation, utility-shortage consequences, spatial land value, zone replacement, incident persistence/resolution, mobility feedback, setters and district aliases, atomic invalid-version rejection, JSON persistence round-trip, recovery restoration, and derived-mobility reset. The existing four-horizon fixture test now compares every newly owned frozen projection rather than only transaction totals. Phase 0 baseline and ten-artifact extraction checks have zero drift. The full `verify.sh` gate passes: export presets validate, the deterministic .NET build has zero warnings, 63/63 domain tests pass, Godot 4.6 completes its headless import, and all 18 foundation integration assertions pass.

Performance/resources: No nodes, bodies, timers, engine callbacks, repositories, or external resources were added. Aggregate projections are deterministic scans over small in-memory authority collections. Published collections and mobility explanations are copied into read-only views; persistence validation materializes complete replacement maps before the single restore commit.

Known deviations and defects: No defects found in the ported surface. C# publishes a typed version-1 state document instead of an untyped JavaScript object; its camel-case JSON round-trip is verified. The browser-only `recordMissionCompletion` call-shape adapter remains with Phase 2.5 item 7, where mission outcome transactions can bind to the ledger without weakening duplicate protection. Alert wording/presentation remains item 6.

Next safe task: Begin Phase 2.5 item 6 with the pure alert service and interaction-priority service, then port their economy/traffic adapters against immutable snapshots. Keep UI rendering and Godot input/raycast adapters outside the domain assembly.
