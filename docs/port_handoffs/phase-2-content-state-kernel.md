# Phase 2 Content and State Kernel Handoff

Chunk ID and status: `phase-2-content-state-kernel`; Complete as the first Phase 2 slice. Phase 2 overall remains in progress.

Source revision / Godot revision: Frozen browser reference `44a286a74557adfe4fabd3a6e16b9006079eba32`; working tree based on Godot revision `f6af37927c34` targeting Godot 4.6 stable .NET.

Objective and explicit non-goals: Establish the C# serialized-name convention, port stable content primitives and MVP scope, load and validate the unchanged production mission JSON, and port immutable game-state policy/request evaluation against Phase 0 fixtures. This chunk does not add a mutable game-state authority, transition coordinator, scheduler, economy, alerts, interactions, mission lifecycle, persistence, world generation, UI, or Godot boot-stage content wiring.

Documents and source files read: `docs/GODOT_4_6_CSHARP_PORT_PLAN.md`, `docs/port_handoffs/phase-1-foundation.md`, `docs/GODOT_PORT_PHASE_1_FOUNDATION.md`, `docs/GAME_STATE_MACHINE.md`, `docs/DATA_VALIDATION.md`, `docs/MVP_SCOPE.md`, `docs/DESIGN_DECISIONS.md`, `src/core/GameState.js`, `src/core/GameTransition.js`, `src/core/GameManager.js`, `src/data/ContentDefinitions.js`, `src/data/DataValidation.js`, `src/data/MissionDataValidator.js`, `src/data/GameDataValidator.js`, `src/data/missions.json`, `src/config/MvpScope.js`, `src/config/FeatureFlags.js`, `src/missions/MissionPolicyDefinitions.js`, `test/GameManager.test.js`, and the Phase 0 `game-state.json` and `content-registry.json` fixtures.

Files/scenes/resources added or changed: Domain code under `godot/MetroPulse.Domain/Core`, `Content`, and `Missions`; matching tests under `godot/MetroPulse.Domain.Tests`; both project files for linked embedded resources; `docs/GODOT_PORT_PARITY_MATRIX.md`; and this handoff. No scene, node, input action, collision layer, export preset, browser source, or authored mission record changed.

Authoritative owners touched: `GameStateCatalog` owns immutable state policies and requested edges; `GameTransitionPolicy` evaluates pure request/destination contracts; `ContentDefinitions` owns the first C# stable primitive/scope view; `MissionValidator` owns mission/dialogue validation; `ContentCatalogValidator` owns primitive graph/scope validation; `MissionRegistry` owns immutable validated mission publication and MVP filtering. The browser JSON remains the mission content authority.

Public APIs, signals, events, input actions, collision layers: Added `GameStateCatalog.Policies`, `GameStateCatalog.Transitions`, `GameStateCatalog.GetTransitionEffects`, `GameTransitionPolicy.EvaluateRequest`, `GameTransitionPolicy.ValidateDestination`, `MissionRegistry.Load`, `MissionRegistry.LoadProduction`, `MissionRegistry.Get`, `MissionRegistry.GetMvpMissions`, `MissionValidator.Validate`, `ContentCatalogValidator.ValidateScope`, and `ContentCatalogValidator.ValidateProgression`. Added no signals, events, input actions, or collision changes.

Stable IDs/schema/content changes: No stable ID, JSON property, fixture schema, mission data, or save schema changed. C# names are PascalCase; JSON properties use explicit camelCase attributes. `GameState` serializes with exact uppercase tokens, including `STREET_ON_FOOT` and `STREET_VEHICLE`. The registry validates all 15 mission IDs, 47 dialogue nodes, 11 vehicle IDs, 4 districts, 7 zones, 4 factions, 3 progression tiers, the 10-ID MVP mission list, and six activity templates. With `temporaryMayhem` off, the valid Mayhem survival mission remains loaded but is excluded from the nine-item default visible set.

Behavior implemented: `src/data/missions.json` is embedded directly into `MetroPulse.Domain.dll` as `MetroPulse.Domain.Content.missions.json`, avoiding a copied content authority. Loading is fail-closed and publishes only read-only/frozen collections after complete validation. Validation covers stable/duplicate IDs, strings, numeric bounds, vehicle/objective/weather/action/retry enums, district references, race/sabotage fields, dialogue target/reachability, prerequisite references/cycles, primitive progression cycles, and MVP scope references. `ContentValidationException` supplies stable code, source, record ID, field, path, user message, and remedy. State policy, requested edges, effects, rejection codes/reasons, destination ownership, pause resume edges, and transient-state behavior match the Phase 0 fixture.

Known deviations and ADR links: None. `DD-016` remains the platform authority. This slice deliberately stops before the mutable `GameManager` and runtime coordinator; the matrix therefore records policy parity as In Progress rather than runtime ownership acceptance.

Tests added and exact commands: Added 12 xUnit cases (20 total domain cases) across `GameStateCatalogTests`, `ContentDefinitionsTests`, and `MissionRegistryTests`. Commands: `./godot/scripts/build.sh`; `./godot/scripts/test-domain.sh`; `dotnet format godot/MetroPulse.Godot/MetroPulse.Godot.sln --verify-no-changes --no-restore`; `npm run baseline:check`; `GODOT_BIN=/private/tmp/Godot46Mono/Godot_mono.app/Contents/MacOS/Godot ./godot/scripts/validate-headless.sh`; and the same prefix with `./godot/scripts/test-integration.sh`.

Test results and artifact paths: Deterministic build passed with zero warnings/errors. All 20 domain tests passed on macOS arm64 in 54 ms; TRX is `godot/artifacts/test-results/domain-tests.trx`. Formatting verification passed. Phase 0 baseline check reported zero mismatches. Godot 4.6 headless import passed, and the unchanged foundation integration runner passed all 18 assertions; raw logs remain under `godot/artifacts/test-results/`.

Performance/resource counts before and after: Pure domain only. No nodes, physics bodies, render resources, timers, subscriptions, or per-frame work were added. Production content publication contains 15 mission records and 47 dialogue nodes.

Manual checks performed: Reviewed the embedded resource name, validated the production registry through tests, checked the Phase 0 exact-fixture comparisons, reviewed the default-off Mayhem filtering result, and inspected the final diff for authored/browser changes. No interactive player-visible behavior exists in this slice.

Open defects with severity and reproduction: None found in the completed slice. Phase 2 exit criteria remain incomplete by design: building/weather/economy/vehicle/camera/world catalogs, scheduler/economy/alert/interaction/outcome/lifecycle/settings/helpers, deterministic streams, and their fixture families are not yet ported.

Compatibility adapters and removal conditions: The linked embedded-resource entry in `MetroPulse.Domain.csproj` is owned by `MissionRegistry`. It preserves the unchanged browser mission JSON during the dual-engine port and remains until the final canonical content packaging pipeline proves byte/checksum parity and supplies the same resource without a second authority. String-form mission prerequisites remain a load compatibility path because the browser validator accepts them; removal requires a content migration and fixture showing no supported record/save depends on that form.

Next safe task: Implement the Phase 2.4 reviewed extraction tool for building catalog and weather/policy definitions, compare every record ID and scalar against `content-registry.json`, then extend `ContentCatalogValidator` to produce the complete production registry before porting mutable state or scheduler services.

Unsafe/blocked tasks and required decision: Do not wire partial content into the Godot boot shell or claim the Phase 2 registry exit gate until buildings and weather are canonical and validated. Do not introduce a mutable Godot node as `GameManager`; the next state slice must remain a pure disposable session service. No external decision currently blocks the next task.
