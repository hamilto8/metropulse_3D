# Phase 2 Pedestrian Kernel Handoff

Chunk ID and status: `phase-2-pedestrian-kernel`; complete as the fifteenth Phase 2 slice. This completes the randomness-independent pedestrian-helper portion of Phase 2.5 dependency item 10; item 10, item 2.6, and Phase 2 overall remain in progress.

Source revision / Godot revision: Frozen browser reference `44a286a74557adfe4fabd3a6e16b9006079eba32`; implementation based on Godot revision `bade3f5` targeting Godot 4.6 stable .NET.

Objective and explicit non-goals: Port bounded pedestrian capsule sweep/slide geometry, vehicle braking/yield encounters, traffic-participant policy, tourist and aggression state transitions, target reservations, and knockdown/terrain/stand-up state. This chunk does not create pedestrian meshes or `CharacterBody3D` nodes, query Godot physics, spawn citizens, transfer control, animate limbs, dispatch police, publish crime, or generate randomized descriptors/appearance.

Documents and source files read: `docs/GODOT_4_6_CSHARP_PORT_PLAN.md`, the Phase 0 and prior Phase 2 handoffs, `src/entities/PedestrianArchetypes.js`, `Pedestrian.js`, `src/systems/PedestrianCollision.js`, `PedestrianImpact.js`, `TrafficPedestrianBehavior.js`, `NpcBehavior.js`, relevant `PedestrianSystem.js` consumers, and matching collision, NPC, traffic-management, and gameplay tests; also the existing canonical pedestrian archetype registry.

Files/scenes/resources added or changed: Added `MetroPulse.Domain/Pedestrians/PedestrianGeometry.cs`, `PedestrianCollisionModel.cs`, `PedestrianTrafficModel.cs`, `NpcBehaviorModel.cs`, and `PedestrianKnockdownModel.cs`; added `PedestrianModelTests.cs`; updated the port plan, parity matrix, and this handoff. No scene, body, mesh, animation, input action, collision layer, browser source, fixture, canonical content record, or save schema changed.

Authoritative owners touched: `GameContentRegistry` remains the canonical archetype sequence/profile owner. `PedestrianCollisionModel` owns bounded planar circle sweeps and oriented-box separation over injected active collider snapshots. `PedestrianTrafficModel` owns yield/braking kinematics, large-vehicle stop clearance, participant eligibility, and patient/impatient encounter state. `NpcBehaviorModel` owns archetype modes, tourist timing transitions, aggression target selection/reservation, and bounded cooldowns using explicit deterministic samples. `PedestrianKnockdownModel` owns throw/lift, gravity/damping, terrain contact, tumble/rest pose, timer, and stand-up recovery. Future Godot adapters own bodies, rendering, input, and side effects.

Public APIs, signals, events, input actions, collision layers: Added immutable pedestrian vector, collider/config/movement, traffic config/kinematics/participant/encounter/action, NPC config/state/candidate/aggression transition, and knockdown config/state records; stable NPC modes; collision move, yield/approach/emergency/participant/encounter helpers; NPC create/advance/select/begin/finish helpers; and knockdown start/update/reset helpers. No event, signal, timer, mutable singleton, input action, or collision layer was added.

Stable IDs/schema/content changes: Preserved collision constants `0.42/2.65/0.2/64/3/0.015`; traffic policy `0.2/3.5/0.18/4.5/7/36/3.5/28/3.1/4.2`; NPC distances/durations `20/1.65/7/1.2/12..22/5..14/5..10/2..4`; stable modes `SITTING_READING/WALKING/TAKING_PHOTO/LOITERING/CHASING/JOGGING`; knockdown `4/0.65/18/3.5/3.25..7.5/2.5..5`, rest roll `1.42`, and tumble `3.5..5.5`. No serialized state was introduced.

Behavior implemented: Movement sanitizes nonfinite displacement, caps each request to `12.8` units, substeps to prevent tunneling, resolves vertical overlap against rotated boxes, and retains tangential wall motion. Traffic detection combines reaction, braking, and clearance; patient drivers yield throughout an encounter while impatient drivers wait, honk once, and release; seated café readers and unavailable bodies are not road hazards. Archetype modes and tourist cycles match authored timing, controlled pedestrians take aggression priority, reservations prevent dog-piling, and finish releases the target with a bounded cooldown. Knockdown uses explicit fall/tumble samples, bounded throw/lift, exponential horizontal damping, terrain contact, smooth stand-up, exact four-second recovery, and safe invalid impact/delta/terrain handling.

Known deviations and ADR links: No behavior deviation requires an ADR. Collider lists are typed immutable snapshots rather than a physics-world lookup, and stable IDs replace JavaScript object identity. Random-dependent operations accept explicit bounded samples; they do not create an anonymous RNG. Randomized appearance descriptors, spawn names/nodes, and runtime behavior rolls are deliberately deferred to item 2.6 named `PedestrianSpawn`/`PedestrianBehavior` streams. A nonfinite starting knockdown position fails immediately instead of propagating NaN.

Tests added and exact commands: Added twelve xUnit cases, increasing the domain suite from 136 to 148. Coverage mirrors browser evidence for building tunneling, tangential sliding, rotated/removed bodies, malformed/extreme movement, four vehicle braking fixtures, exact 20% disposition, patient/impatient honk behavior, participant/emergency policy, archetype/tourist timing, controlled/nearest/reserved aggression selection, begin/finish reservation lifecycle, bounded knockdown launch, invalid input safety, and complete standing recovery. Commands: `dotnet test godot/MetroPulse.Domain.Tests/MetroPulse.Domain.Tests.csproj --no-restore --filter FullyQualifiedName~PedestrianModelTests`, `dotnet test godot/MetroPulse.Domain.Tests/MetroPulse.Domain.Tests.csproj --no-restore`, `dotnet format godot/MetroPulse.Godot/MetroPulse.Godot.sln --no-restore --verify-no-changes`, `npm run baseline:check`, `npm run godot:content:check`, and `GODOT_BIN=/private/tmp/Godot46Mono/Godot_mono.app/Contents/MacOS/Godot ./godot/scripts/verify.sh`.

Test results and artifact paths: The focused pedestrian suite passes `12/12` and the domain suite passes `148/148`. The full verification gate exits `0`: export preset identifiers validate; canonical extraction and Phase 0 baseline checks have zero drift; the deterministic .NET build completes with zero warnings and errors; Godot 4.6 stable .NET completes headless import; and all `18` foundation integration assertions pass. TRX output is `godot/artifacts/test-results/domain-tests.trx`.

Performance/resource counts before and after: Domain tests increased from 136 to 148. Runtime node/body/timer/input/collision counts remain unchanged at zero for this assembly. Yield, encounter, NPC transitions, and knockdown stepping are constant-time; target selection is linear in candidates; collision movement is bounded by 64 substeps × 3 resolution passes × active collider count.

Manual checks performed: None; this slice has no player-facing or engine-owned surface. Character movement feel, live collider snapshots, animation/readability, control handoff, spawning/population floor, crime/police response, and soak counts remain later phases.

Open defects with severity and reproduction: None found in the ported surface.

Compatibility adapters and removal conditions: Active collider snapshots are the boundary until Phase 4/5 collision owners translate authoritative Godot shapes. Explicit behavior samples are the boundary until item 2.6 named streams supply them. Godot pedestrian/traffic adapters must consume the returned movement and state records without duplicating formulas. Browser pedestrian modules remain compatibility evidence until live movement, collision, traffic-yield, and knockdown scenarios pass.

Next safe task: Continue Phase 2.5 item 10 with `PlacementIntelligence` pure geometry and explanation contracts, followed by the pure aircraft flight and landing-surface helpers. Then implement item 2.6 named deterministic streams and use them for pedestrian descriptor/behavior generation.

Unsafe/blocked tasks and required decision: Do not instantiate `CharacterBody3D`, read a Godot physics world, mutate meshes/limbs, spawn citizens, publish crime, or transfer player control from domain code. Do not introduce `Random.Shared` or an unnamed `System.Random`. No external decision blocks the placement slice.
