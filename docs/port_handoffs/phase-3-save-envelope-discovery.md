# Phase 3 Save Envelope and Discovery Handoff

Chunk ID and status: `phase-3-save-envelope-discovery`; complete as the fifth Phase 3 slice. Phase 3 remains in progress.

Source revision / Godot revision: Frozen browser reference `44a286a74557adfe4fabd3a6e16b9006079eba32`; implementation continues from Godot revision `0bc5236` and targets Godot 4.6 stable .NET.

Objective and explicit non-goals: Implement the exact native versioned save envelope, sequential migrations, future-version rejection, whole-document validation, discovery/action semantics, save debounce/status policy, and production boot repository wiring. This slice retains but does not apply restore descriptors; static/runtime restore, browser import, player-selectable boot presentation, live session capture subscriptions, and expanded diagnostics remain separate Phase 3 work.

Documents and source files read: Complete Phase 3 plan; `VERSIONED_SAVE_SERVICE.md`; frozen save fixtures; browser `SaveSchema.js`, `SaveGameState.js`, `SaveService.js`, `SaveDiscovery.js`, and `IndexedDbSaveRepository.js`; native content/settings/alerts/mission authorities, boot composition, repository, diagnostics runner, and prior Phase 3 handoffs.

Files/scenes/resources added or changed: Added `GameSaveContracts`, `GameSaveDocumentValidator`, `GameSaveDiscovery`, and `GameSaveService`; embedded nine frozen save fixtures in the domain-test project; added `GameSavePersistenceTests`; wired the production validator/repository plus save-discovery and save-application stages into `CompositionRoot`; expanded headless integration; updated the port plan, save contract, parity matrix, and this handoff. No browser source, scene topology, input action, collision layer, canonical content record, or fixture changed.

Authoritative owners touched: `GameSaveDocumentValidator` exclusively migrates, validates, normalizes, inspects, and creates aggregate envelopes. `GameSaveService` owns scheduling, checkpoint metadata, capture-to-envelope publication, repository commits, and save-status snapshots. `GodotGameSaveRepository` remains storage-only. `GameSaveDiscovery` owns slot eligibility and prepare semantics. `CompositionRoot` owns production construction and the retained `PreparedBootSave`; absent world/entity authorities are not simulated.

Public APIs, signals, events, input actions, collision layers: Added constants for format/schema/feature, slots, boot actions, and autosave reasons; validation/discovery/prepared-save/status records; `GameSaveDocumentValidator.Validate`, `Create`, and `Inspect`; `GameSaveDiscovery.Discover` and `Prepare`; and `GameSaveService.ScheduleSave`, `Advance`, `SaveNow`, `SaveCheckpoint`, and status subscription. Boot now emits running/complete events for all nine declared stages. No Godot signal, InputMap action, autoload, or collision layer changed.

Stable IDs/schema/content changes: Native support now explicitly preserves `METROPULSE_3D_SAVE`, schema version 2, feature version 2, twelve required domains, current/recovery slot IDs, `NEW_GAME`/`CONTINUE`/`RECOVER`, and frozen autosave reason/checkpoint metadata. Sequential schema 0→1→2 migration records `P4.1_ZONE_VOCABULARY` and canonicalizes legacy `INDUSTRIAL`/`OFFICE` zone aliases. No stable authored content ID or frozen fixture changed.

Behavior implemented: The entire envelope migrates and validates before publication or mutation. Validation covers game/player/world/time-weather/missions/factions/progression/heat/settings/bindings/alerts plus stable content and controlled-entity references. Frozen current, controlled-entity, mid-mission, result, and recovery saves validate. Corrupt and future documents classify actionably. Save creation deduplicates reasons, retains stable checkpoints, and round-trips all domains. The service coalesces a deterministic five-second debounce, saves checkpoints immediately, honors mission save gates, contains repository/listener failures, and publishes immutable status. Boot discovers production slots before construction, deterministically prefers Continue then Recover then New Game, prepares the selected slot after construction, and releases input only after final readiness.

Known deviations and ADR links: No parity deviation requires an ADR. The browser lifecycle validator intentionally treats the nested result receipt as opaque; native aggregate validation mirrors that shared save contract while the richer C# outcome service continues to enforce its own receipt model when restoring its authority. The native save debounce is advanced by the session host instead of a worker timer so Godot-owned capture cannot run off-thread; this preserves elapsed-runtime behavior and prevents offline catch-up.

Tests added and exact commands: Added 13 persistence cases covering all nine frozen fixtures, sequential migrations, zone aliases, future/corrupt classification, cross-domain/content/settings rejection, exact envelope creation, discovery/prepare semantics, reason coalescing/status/mission gates, repository failure containment, and observer isolation. Expanded native integration from 60 to 67 assertions for the nine-stage boot order, production validator/repository/discovery ownership, clean action eligibility, retained New Game descriptor, and isolated slot state. Commands: `dotnet test godot/MetroPulse.Domain.Tests/MetroPulse.Domain.Tests.csproj --filter FullyQualifiedName~GameSavePersistenceTests`; `dotnet format godot/MetroPulse.Godot/MetroPulse.Godot.sln --no-restore --verify-no-changes`; `npm test`; and `GODOT_BIN=/Applications/Godot_mono.app/Contents/MacOS/Godot ./godot/scripts/verify.sh`.

Test results and artifact paths: Native build succeeds with zero warnings/errors; focused persistence passes 13/13; browser tests pass 389/389; domain tests pass 207/207; formatting passes; and all 67 headless integration assertions pass. The final complete verifier exits 0 through export-preset validation, canonical-content drift, the 62-file ownership audit across 77 domain files, build, domain tests, headless import, and integration. Test artifacts remain under `godot/artifacts/test-results/`.

Performance/resource counts before and after: Boot stage count grows from seven to nine and progress events from fourteen to eighteen. One validator, repository, discovery report, and prepared descriptor are session-shell process owners; no node, timer, body, render resource, collision object, listener, or per-frame task was added. The save service allocates and writes only on explicit schedule advancement or immediate save calls.

Manual checks performed: Inspected normalized frozen envelopes and headless structured boot logs, including nine ordered stage pairs and the clean `NEW_GAME` descriptor. No player-facing save picker/status UI exists in this slice, so visual, accessibility, and physical-device checks remain deferred.

Open defects with severity and reproduction: None found in this slice. Continue/Recover are eligible and prepared but static/runtime owner application is deliberately deferred; the current automatic priority policy will be replaced by selectable boot presentation/CLI paths before Phase 3 exit.

Compatibility adapters and removal conditions: Browser save modules and frozen fixtures remain compatibility evidence. The result-receipt opacity adapter remains only while supported schema-2 browser saves may contain the compact receipt; remove it only with a sequential save migration and restore fixture. Deferred `PreparedBootSave` retention must remain explicit until static and runtime restore adapters prove atomic apply/rollback.

Next safe task: Split restore into static domain application and deferred runtime entity/world descriptors, with transaction rollback and frozen Continue/Recover coverage; then implement browser export/import backup, preview, confirmation, and failure isolation.

Unsafe/blocked tasks and required decision: Do not serialize Godot objects, mutate any owner before full validation, fabricate absent runtime entities, apply half a restore, erase a valid current during discovery, or report a retained descriptor as a completed restore. No external decision blocks the next slice.
