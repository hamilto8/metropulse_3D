# Phase 7 Economy and Catalog Handoff

Chunk ID and status: `phase-7-economy-catalog`; complete as the first of five Phase 7 slices. Work item 7.1 and the catalog/vocabulary portion of 7.2 are complete; Phase 7 remains in progress.

Source revision / Godot revision: Frozen browser reference `44a286a74557adfe4fabd3a6e16b9006079eba32`; follows Godot revision `290389c`; targets Godot 4.6 stable .NET `89cea1439`, Jolt, and the existing 120 Hz physics policy.

Objective and explicit non-goals: Establish the live economy authority, immutable Management projection, authored skyline registration, and exact catalog access policy before world editing. This slice does not create editor tools, mutate runtime world geometry, register user roads, handle incidents, change persistence, or claim a Phase 7 exit gate.

Documents and source files read: `docs/GODOT_4_6_CSHARP_PORT_PLAN.md`, `CITY_ECONOMY_ARCHITECTURE.md`, `CONSTRUCTION_VOCABULARY.md`, `PLACEMENT_INTELLIGENCE.md`, `ECONOMY_RECOVERY_AND_BALANCE.md`, the Phase 6 exit handoff, `EconomySystem.js`, `BuildingEconomyAdapter.js`, `ConstructionVocabulary.js`, `BuildingCatalog.js`, `BuildingFactory.js`, `CityBuilder.js`, and their browser tests and frozen Phase 0 economy fixture.

Authorities and data flow: `CityEconomyRuntime` is session-owned and contains the sole live `EconomyLedger`. `BuildingEconomyAdapter` is the only catalog/authored-world-to-economy mapping boundary. `CityEconomyViewModel` is an immutable projection and never owns mutable counters. The runtime registers `economy.city-tick` at `SimulationStage.City`, so pause and Management/Builder/Street clock policy remain scheduler-owned.

Reference baseline and skyline behavior: Before authored registration the live authority matches the frozen oracle exactly: 650,000 treasury, 1,200 population, 70 happiness, 100 land value, full zero-demand power/water/fire coverage, 8 credits/second gross and net income, and zero upkeep. The 23 `InitialSkyline` building definitions then become stable `existing-1` through `existing-23` records through the same adapter family used by the catalog. Their deterministic employee/value proxies are financially neutral, positioned, and do not alter the reference treasury, population, happiness, land value, service coverage, or rate.

Catalog/content contract: All 19 canonical records remain content-owned. The policy publishes exactly the three development zone IDs, five construction categories, `STARTER`/`ADVANCED` disclosure stages, and `OPERATOR`/`BROKER`/`MAGNATE` tiers. Default disclosure contains the exact six documented starter assets. Advanced Operator records become available only when explicitly disclosed; Broker and Magnate records remain visible-but-locked unless the caller requests unlocked-only results. `IND`/`INDUSTRIAL` normalize to `OPERATIONS`, and `OFFICE` normalizes to `COMMERCIAL`; legacy service zone IDs remain load-only compatibility values.

Public API and schema changes: Added catalog filtering/access/normalization APIs, catalog and skyline economy adapters, the Management view model, and the live economy runtime. No input action, collision layer, save envelope, canonical JSON content, browser source, or scene resource changed. Godot assembly informational version is now `0.1.0-phase7`.

Verification: New domain tests cover exact vocabulary/disclosure/tier behavior, canonical financial/service mapping, all 23 skyline records, and direct Phase 0 scalar parity. The full domain suite passes 285/285. The Godot build has zero warnings/errors. Clean, confirmed-import, and recovery integration runs pass with 160/160/164 assertions; rejected unconfirmed, corrupt, and future imports retain their expected errors. The live check proves one scheduler-owned city tick adds exactly eight Capital and republishes the immutable view.

Known deviation: The browser's non-catalog skyline creates eight late generic towers with `Math.random()` employee counts, so no stable employee oracle exists for those records. Godot uses the existing deterministic Phase 4 height and the browser fallback rule of eight employees per height metre for every authored skyline node. This affects only deterministic aggregate employment/value proxy metadata; treasury, population, service state, happiness, land value, and recurring balance remain exact. This intentional variance will be reported in the final economy parity artifact.

Next safe task: Implement the editor command API, aim/grid/catalog state, live terrain-conforming preview and blocker projection, zoning, place/move/rotate/demolish/cancel commands, and reverse-order compensation across every participant. The editor must call `ConstructionVocabulary.GetCatalogAccess` server-side and `BuildingEconomyAdapter.FromCatalog`; it must not duplicate either policy.
