# Phase 2 Traffic Kernel Handoff

Chunk ID and status: `phase-2-traffic-kernel`; complete as the thirteenth Phase 2 slice. This completes the renderer-independent navigation/rule and aggregate-productivity portion of Phase 2.5 dependency item 10; item 10 and Phase 2 overall remain in progress.

Source revision / Godot revision: Frozen browser reference `44a286a74557adfe4fabd3a6e16b9006079eba32`; implementation based on Godot revision `fd1cca5` targeting Godot 4.6 stable .NET.

Objective and explicit non-goals: Port pure road-segment projection, target-plane reach, turn-speed, signal timing, driver/control planning, stopping kinematics, aggregate road productivity, bridge policy, mission/street directives, economy feedback, persistence, and structured traffic alerts. This chunk does not create road-node scenes, vehicle actors, lane-snapping mutation, raycasts/collider queries, collision response, traffic spawning, path selection, traffic lights, overlays, or Godot nodes.

Documents and source files read: `docs/GODOT_4_6_CSHARP_PORT_PLAN.md`, the Phase 0 and prior Phase 2 handoffs, `src/systems/TrafficNavigation.js`, `TrafficRules.js`, `TrafficProductivityModel.js`, `TrafficAlertAdapter.js`, `test/TrafficNavigation.test.js`, `TrafficProductivityModel.test.js`, and the existing C# economy, alert, outcome, condition, canonical vehicle, and economy-policy contracts.

Files/scenes/resources added or changed: Added `MetroPulse.Domain/Traffic/TrafficNavigationModel.cs`, `TrafficRulesModel.cs`, `TrafficProductivityContracts.cs`, and `TrafficProductivityModel.cs`; added `Alerts/TrafficAlertAdapter.cs` and `TrafficModelTests.cs`; updated the port plan, parity matrix, and this handoff. No scene, input action, collision layer, browser source, fixture, canonical content record, or save-envelope schema changed.

Authoritative owners touched: `TrafficNavigationModel` owns pure planar segment/reach/turn geometry. `TrafficRulesModel` owns signal cycles/states, stable driver profiles, authored control plans, approach parsing, and stopping kinematics. `TrafficProductivityModel` is the sole aggregate road-mobility owner over injected `EconomyLedger`, optional `MissionOutcomeService`, and road snapshots; it publishes immutable productivity, bridge, job-access, delivery, mission, street, policy, and presentation values back through `EconomyMobilityFeedback`. `TrafficAlertAdapter` observes that authority and publishes deduplicated alerts. Visible vehicles remain presentation samples and must never become productivity input.

Public APIs, signals, events, input actions, collision layers: Added immutable traffic points/configuration/projection/control/approach/kinematics records; signal/control/access/policy stable tokens; road/productivity/mission/street/state records; navigation/rule helpers; bridge-policy set/toggle/options; update/snapshot/subscribe; mission/street queries; versioned serialize/restore/validate; and traffic alert synchronization/disposal. No engine signal, input action, collision layer, or mutable singleton was added.

Stable IDs/schema/content changes: Preserved navigation constants `1.5/7/3.5/0.35/18/5.5/7/22/0.45`; traffic-rule constants `0.8/9/2/1/1.1/18/34/0.3/4`; `RED/YELLOW/GREEN`, `SIGNAL/STOP`, `BALANCED/FREIGHT_PRIORITY`, and `OPEN/RESTRICTED/CLOSED`; primary bridge ID/district and X range `100..210`; policy multipliers/penalties/tradeoff copy; productivity state version `1`; and all alert dedupe keys. The state is a component document for the future Phase 3 save envelope, not a new envelope version.

Behavior implemented: Projection clamps to a segment and rejects degenerate geometry; target reach detects proximity or overshoot; imminent turns reduce speed. Signal axes alternate with both all-red intervals; deterministic fifth drivers are reckless; countryside side roads become four-way stops; approach IDs and stopping distances match the browser. Aggregate demand combines workforce/jobs and commercial/operations demand; connected road edits add capacity while disconnected edits create explainable hotspots; outcome traffic, infrastructure, and outages drive access/hazard/congestion; freight priority improves bridge capacity/reliability for explicit Capital and satisfaction costs; snapshots drive economy mobility feedback; bridge closures block crossing missions and stop bridge presentation; unchanged inputs retain revision identity; alerts expose network, bridge, and delivery consequences.

Known deviations and ADR links: No behavior deviation requires an ADR. Typed C# inputs replace JavaScript missing-object checks, and invalid nonfinite navigation inputs fail safely. Outcome outage severity remains bounded by the already-authoritative `MissionOutcomeService` `0..1` contract; tests compose valid city restriction and outage records instead of bypassing that validator. C# state restoration accepts only validated stable policy tokens. Engine-owned lane-corridor mutation and obstacle queries are intentionally deferred, not behavioral substitutions.

Tests added and exact commands: Added ten xUnit cases, increasing the domain suite from 114 to 124. Coverage includes exact frozen baseline productivity scalars, segment projection/reach/degeneracy, turn limits, signal/all-red timing, control/driver/approach tokens, stopping kinematics, road capacity/hotspots/no-op revision behavior, freight tradeoff/persistence/economy cost, closure mission/street effects, and structured alert consequences. Commands: `dotnet test godot/MetroPulse.Domain.Tests/MetroPulse.Domain.Tests.csproj --no-restore --filter FullyQualifiedName~TrafficModelTests`, `dotnet test godot/MetroPulse.Domain.Tests/MetroPulse.Domain.Tests.csproj --no-restore`, `dotnet format godot/MetroPulse.Godot/MetroPulse.Godot.sln --no-restore --verify-no-changes`, `npm run baseline:check`, `npm run godot:content:check`, and `GODOT_BIN=/private/tmp/Godot46Mono/Godot_mono.app/Contents/MacOS/Godot ./godot/scripts/verify.sh`.

Test results and artifact paths: The focused traffic suite passes `10/10` and the domain suite passes `124/124`. The full verification gate exits `0`: export preset identifiers validate; canonical extraction and Phase 0 baseline checks have zero drift; the deterministic .NET build completes with zero warnings and errors; Godot 4.6 stable .NET completes headless import; and all `18` foundation integration assertions pass. TRX output is `godot/artifacts/test-results/domain-tests.trx`.

Performance/resource counts before and after: Domain tests increased from 114 to 124. Runtime node/body/timer/input/collision counts remain unchanged at zero for this assembly. Navigation/rule helpers are constant-time except control-plan creation (`O(x*z)`). Productivity calculation is linear in road segments plus outcome traffic/outage records; subscriber work is linear in listener count.

Manual checks performed: None; this slice has no player-facing or engine-owned surface. Road graph visualization, live traffic controls, vehicle corridor behavior, visible sampling, and alert/UI presentation remain later phases.

Open defects with severity and reproduction: None found in the ported surface.

Compatibility adapters and removal conditions: The injected road snapshot is the boundary until the Phase 6 shared Godot road graph implements the same contract. `EconomyMobilityFeedback` remains the deliberate aggregate bridge into the economy authority. `TrafficAlertAdapter` resolves the deferral recorded in `phase-2-alert-interaction-kernel.md`; it remains an observer, not a second traffic owner. Browser traffic modules remain compatibility evidence until Godot runtime adapters consume these contracts without duplicating formulas.

Next safe task: Continue Phase 2.5 item 10 with the pure camera model helpers, which can consume the already-canonical camera presets without scene, input, raycast, or camera-node ownership. Then port dependency-safe pedestrian/placement/flight helpers around their existing canonical content.

Unsafe/blocked tasks and required decision: Do not port `enforceLaneCorridor`, `findTrafficObstacleAhead`, `TrafficControlSystem`, vehicle separation, road-graph mutation, spawning, or visible traffic sampling into the domain assembly; those require Phase 6 Godot scene/physics/graph owners. Do not let ambient vehicles feed aggregate congestion. No external decision blocks the next pure-helper slice.
